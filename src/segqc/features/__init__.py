"""segqc.features — geometric and topological feature extractors (Stage 2/3).

Each sub-module exposes one principal public type and a matching compute
function; no heavy state is shared between them.
"""
