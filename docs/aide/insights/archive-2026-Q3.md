# Insight Archive — 2026-Q3

_Closed entries moved out of `insights.md` by `aide insights archive`._
_Frozen: the claims are immutable and, unlike the live file, this one is_
_not shape-checked — see `insight_warnings`._

- [x] defect — item 101's AC16 demonstrator (`test_ac16_attributed_mode_is_three`) is mathematically at risk of an exact tie: with the real `mode3_inject_islands` fixture, stripping stray components reconstructs the candidate to *exactly* GT, so modes 1 (`unanchored_foreground_fraction`), 2 (`min_dominant_component_fraction`) and 3 (`rogue_island_count`) all independently return to their own baseline, making `abs(normalised_delta) == 1.0` for all three (verified bit-exact via `.hex()`) regardless of how small each mode's real-world departure was; `compare_runs`'s spec'd/tested tie-break ("ties to the lowest mode", AC13) then picks mode 1, not 3, on this exact input — a structural consequence of the documented `scale = max(|value_a-baseline|, |value_b-baseline|)` formula (any metric that fully resolves to baseline saturates to ±1.0 irrespective of magnitude) that the spec's Assumptions log did not anticipate colliding across multiple modes on this specific "perfect fix" fixture; needs either a fixture tweak (leave a residual departure in modes 1/2) or a secondary tie-break, decided by whoever owns the spec/tests *(item 101, 2026-07-27)* → superseded by the fuller 2026-07-27 entry below (same root cause, wider than the AC16 fixture); both routed to **Stage 26 — Carried-Defect Remediation** candidate **D1** (make `normalised_delta` magnitude-sensitive instead of saturating at ±1.0 whenever one run sits on baseline)
- [x] framework — `/aide-review-permissions` and `.claude/scripts/review_permissions.py` still tell the human to add promoted rules to `.claude/settings.json`, which is a *generated* artifact once `settings.overlay.json` is adopted; they should target `permissions.allow.add` in the overlay instead *(2026-07-22)* → fixed in aide-loop PR #8, shipped in engine 1.2.0 and installed here
- [x] knowledge — a stage validation must resolve `[validation]` profiles per machine via `aide env --profile <name>` and record ❓ Unverified on a non-zero exit; never assume a capability from a previous run's result, which is local state and is deliberately not stored *(2026-07-22)* → already captured, no edit needed: `CLAUDE.md` § "Project configuration" states the ❓ Unverified rule, and `aide.toml`'s `[validation]` comment block states that the per-machine answer is computed at runtime and deliberately never stored
- [x] defect — `CLAUDE.md` links to `.aide/README.md` three times (lines 5, 170, 258) but the installer never creates that file: `.aide/` holds only `conventions.md`, `VERSION`, `templates/`, `scripts/`, `loop/`. The loop/agents/merge-policy content those links promise lives in aide-loop's own `README.md`, which a consumer does not receive — so either point them at `.aide/conventions.md` or have the framework install a `core/README.md` *(2026-07-23)* → aide-loop issue #16, recommending `core/README.md`. Root cause found by archaeology: the file *did* exist here (created `cd93e1b`, deleted `2e7152d` when the standalone framework was adopted) and its sections match the three links exactly — the extraction dropped it, so this is a framework regression, not a project authoring slip → ticked as handed over: aide-loop issue #16 is filed and recommends `core/README.md`; nothing further is owed by this repo, the entry closes when the framework ships it
- [x] framework — `_parse_item_status` treats *every* "Item NNN" mention in `progress.md` as a status reference, inheriting whatever icon terminates the line (or the enclosing bullet), so prose that merely *discusses* an item is read as declaring its status: items 044/047 resolve to 🚧 because the environment-gated table's Notes cell name-drops them and ends "G3 stays 🚧", and items 060/070 resolve to planned via a Notes cell with no trailing icon and via `- [x]` acceptance checkboxes. That is what makes queue-005/006/007/008 warn "marked completed but still has open items". Restrict the parse to the documented `*(Item NNN)*` reference on a deliverable bullet *(2026-07-23)* → aide-loop issue #15. Partly mis-attributed: the queue warnings were the multi-item parse bug (aide-loop PR #12), not this; the prose-mention behaviour is real but latent, now that correct bullet references outrank it
- [x] defect — items 056, 062 and 064 have no `*(Item NNN)*` reference anywhere in `progress.md`: they shipped but were never wired to a deliverable bullet, so they read as planned forever, hold queue-006/007 open, and are invisible to the queue-planner. `aide progress set` now hard-errors on exactly this, so back-fill the references *(2026-07-23)* → **RETRACTED, wrong diagnosis** — all three *are* referenced, correctly, on ✅ deliverable bullets (L322, L372, L373) in the documented `*(Items A, B)*` multi-item form. No project edit is needed; the framework parser cannot read them. Superseded by the multi-item-reference entry below
- [x] framework — 54 of `aide check`'s 60 warnings here are "status icon outside a structural status position" fired on narrative prose ("**G3 stays 🚧**"), which `conventions.md` §1 explicitly permits ("authors need not avoid the icon vocabulary in free text"). The warning fires precisely when the convention is being followed, and the noise buries the six substantive warnings — narrow it or drop it *(2026-07-23)* → aide-loop issue #13
- [x] framework — no way to express a stage whose deliverables all shipped but whose *goal* was not met and is deliberately held open. Stage 14 (G3: real FPR ≤ 0.10, actual 0.975/1.0) is honestly 🚧 with every deliverable ✅, so the rollup warns "all deliverables ✅ but summary shows in-progress" permanently and the only way to silence it is to claim a success that did not happen. The deferred rework is not an existing deliverable, so no bullet can carry ⏸️ instead *(2026-07-23)* → aide-loop issue #14, **resolved** by the Outcome targets mechanism (aide-loop PR #19, engine 1.4.0, installed here): a `## Outcome targets` table whose rows gate the objective, not the stage. Applied to this repo — Stage 14 is now ✅ with its G3/G7 FPR/sensitivity goals recorded as two `❌ Not met` Outcome-target rows
- [x] gap — rework the `reference_delta` rule so its threshold is derived from the training cohort's own `robust_z`/`distribution_distance` percentiles (as `bounds`/`fragmentation` already do) instead of a single hand-set/grid-searched constant; the fixed-constant z-score mechanism cannot clear the G3 held-out FPR ≤ 0.10 target without dropping real-GT `fragment`/`sequence_break` sensitivity below the anti-gaming floor (the two Stage-14 Outcome targets that stay ❌ Not met until this lands) *(2026-07-23)* → routed to the **Stage 23** placeholder (multivariate normative model) in the 2026-07-25 supersession; the two ❌ Outcome targets now name Stage 23 as the carrier. Deliberately not specified further until real-segmenter measurements exist
- [x] framework — **the loop is wedged: `aide claim` reports "none left" and cannot reach a future queue.** `_parse_item_status`'s `[Ii]tem[s]?\s+0*(\d+)` captures only the FIRST number in a `*(Items A, B)*` reference — the exact multi-item form `aide-create-queue` requirement 8 tells authors to write — so items 044, 047, 056, 060, 062, 064 and 070 read as unreferenced despite sitting on ✅ deliverable bullets. That holds queues 005–008 open, makes queue-005 the "live" queue, and with `claim_scope = "live-queue"` every `aide claim` scopes to it and finds nothing claimable; a new queue-013 would be invisible. The same file already has the correct semantics in `_item_ref_re` (`\bItems?\b[^)]*\b0*NNN\b`, used by `aide progress set`, docstring: "Match `*(Item NNN)*` / `*(Items 006, NNN)*`"), so the two functions disagree about what "item NNN is referenced" means. Fix `_parse_item_status` to attribute a line's status to *every* number inside an `*(Items …)*` group *(2026-07-23)* → fixed in aide-loop PR #12 (engine 1.3.1, installed here): both call sites now share one definition, ranges are expanded, and all twelve queues read done again
- [x] framework — `command_hygiene_guard.py`'s rule 1 ("drop the `cd`/`git -C` prefix") grants exactly one exception — `[framework] local_path` from `aide.toml` — so it refuses *every* git operation against any other repository, including directories the session has explicitly declared as additional working directories. That bakes in the assumption that the consumer repo is the only repo an agent ever touches, which fails as soon as one project spans two repos developed together: here the `segfacet` library and a sibling programme repo, where an agent could `git init` the sibling and write files into it (both take a path argument) but could not `git -C <sibling> add`/`commit` them — leaving rescued documents uncommitted for the human to finish by hand. The guard exists to prevent permission-prompt stalls, not as a trust boundary, so the narrow fix is to widen the exception to declared additional working directories, or add an explicit allow-list key (e.g. `[agent] extra_repos`). Note the current shape blocks the honest spelling while leaving `--git-dir`/`--work-tree` open, so it reads as a lint that an agent is rewarded for evading *(2026-07-25)* → **(pending handover, 2026-08-11)** — to be filed against `dadrobny/aide-loop` ([framework] repo); not filed at triage time, no `gh` on the triage host → **resolved: aide-loop 1.12.0** — `[hygiene] extra_repos` in the personal `.aide/loop/loop.local.toml` (declared here for `spine-failure-lab`, `SPINEPS`, `TPTBox`); the key relaxes the lint only and grants no permission, and two different repos in one command stay blocked
- [x] knowledge — `CLAUDE.md` said items 042/045/063 "assert byte-identical output against committed golden files … which only holds within that pinned numpy/scipy environment". They do not. Every `read_bytes()` comparison in those tests is **run-to-run inside one session** (`dest1` vs `dest2`) — a *determinism* check. Comparison against the **committed** artifacts goes through `reports_close`, a **numeric-tolerance** comparison deliberately relaxed by item 078 because full-precision floats differ ~1 ULP across platforms. Consequences: `constraints.txt` is far less load-bearing than documented, and a numpy-major change is safe in a way the doc denied — confirmed empirically, the suite is green on numpy 1.26.4 having previously been pinned to 2.0.2 *(2026-07-25)* → `CLAUDE.md` paragraph corrected in the supersession commit; the distinction also underwrites Stage 19's golden-retirement decision
- [x] gap — the synthetic corpus overstates coverage in two measurable ways: **6 of 10 registered rules fire on zero cases** (`bounds`, `mislabel`, `overlap`, `intensity`, `reference_delta`, `intensity_reference_delta`), and **3 of 9 cases fire nothing at all** through `run_qc` (modes 1/4/8 reach their rule only via a hand-reconstructed record — item 040's documented limitation), so 3 of 8 failure modes are not detected end-to-end while the corpus reads as covering all eight. Separately `verify_case` never asserts that *no unintended* rule fires; cross-talk is 0/9 today, which makes the assertion free to adopt now and expensive once cases become realistic *(2026-07-25)* → scoped as **Stages 19–21** in the supersession, but recorded here because the measurement predates the stages and should not be lost if they are re-planned → **Stage 20**, whose deliverables now name this measurement explicitly. Reframed by the 2026-08-11 Stage-20 traceability clarification (`roadmap.md`): the load-bearing half is the *rule* count (a registered rule firing on zero cases is a coverage finding); an *unwired feature* is legitimate inventory, not a gap
- [x] gap — `segfacet.features.geometry`'s `touches_inferior/superior/left/right/anterior/posterior` face-mapping is documented in its own docstring as "a pragmatic convention for tools that work in any orientation without a reliable RAS header; downstream callers that have orientation information can remap as needed" — i.e. `x==0`/`x==shape[0]-1` are treated as the cranio-caudal axis regardless of what the volume's actual affine says, which only happens to be true for the hand-built synthetic corpus (constructed with the spine along array axis 0) and is not generally true for real, arbitrarily-oriented NIfTI data. Item 094 (Stage 17) makes every loaded volume's array layout consistently RAS-oriented (via TPTBox `reorient`), which is the *prerequisite* for this remap but deliberately does not perform it (scoped to `segfacet.io` only). A follow-on item should audit `geometry.py`'s face-mapping table plus every rule that consumes `touches_*` (`heuristics/border.py`, `heuristics/fov.py`, item 089) against true RAS anatomy and correct the mislabelling *(item 094, 2026-07-26)* → **Stage 26** candidate **D2**, and upgraded from latent to live: item 094 now reorients every loaded volume to RAS (`io.py:166`), so `x == 0 → touches_inferior` (`features/geometry.py:251-256`) names the left-right axis for anything read through `segfacet.io` — `border`/`fov` findings on real data are mislabelled by construction, not merely by convention
- [x] knowledge — item 093 renamed `segfacet.labels.DEFAULT_LABEL_MAP`/`CANONICAL_ORDER` off the old `S`/`Cocygis` sacral names onto the TPTBox `L6`/`S1`-`S6`/`Cocc` range, but `src/segfacet/heuristics/bounds.py` (lines ~44, ~53, ~59) still comments "S and Cocygis are intentionally omitted (unbounded)" — the *behaviour* is unaffected (`_LEVEL_GROUP` is derived generically from `CANONICAL_ORDER` by name prefix, so `S1`-`S6`/`Cocc` are still correctly omitted as unbounded), but the comment text now names labels that no longer exist in the convention and should be refreshed to `S1-S6`/`Cocc` for a future reader *(item 093, 2026-07-26)* → **Stage 26** candidate **D6** (comment-only). Deliberately not fixed inline in this feedback loop: item 105's AC14 pins a whole-tree `src/segfacet/**` digest, so even a comment edit must land inside an item authorised to re-pin it
- [x] framework — `_item_dependencies` (`.aide/scripts/aide.py:1193-1206`, used by `aide claim`'s `_pick_item` to decide whether an item is blocked) extracts blocking dependencies via `re.findall(r"\bItem[s]?\s+0*(\d+)", section)` over the ENTIRE `## Dependencies` section text, with no case-insensitivity and no directionality check — it cannot distinguish "this item depends on Item NNN" from "Item NNN depends on this" or from a lowercase "item NNN" mention (which it silently misses entirely, since there is no `re.IGNORECASE`). Concretely on queue-013: item 097's Dependencies section reads "Items 093, 094, 095, 096" — the regex only captures the first number after "Items" (093), so 094/095/096 were never registered as blockers, and `aide claim` offered item 097 as claimable the moment 093 (alone) went ✅, while 094/095 were still 📋 — i.e. before the stage's actual work existed. Separately, item 095's Dependencies section's forward-looking note "**Item 097** (stage validation) depends on this item's CI job" was mis-read as 095 depending on 097 (backwards), which is *why* `aide claim` skipped 095 in favour of 096 earlier in the same run. This is the same class of bug the multi-item-reference entry above (`_parse_item_status`, fixed in aide-loop PR #12 / engine 1.3.1) already fixed for progress.md parsing — `_item_dependencies` needs the same fix (expand every number in an `*(Items A, B, C)*`-style group) plus a directionality guard so a "depends on this item" forward-reference doesn't register as a backward blocker. Caught by manual review before any work started on the wrongly-unblocked item, not by the tool *(2026-07-26)* → fixed in aide-loop PR #21, shipped in engine 1.4.2 and installed here: `_item_dependencies` now reuses the shared `_referenced_item_numbers` extraction and stops scanning at a documented `**Downstream` marker
- [x] defect — item 095's new `test-numpy-majors` CI job (`.github/workflows/ci.yml`) runs the **full** `python -m pytest` suite per numpy leg, which on GitHub's `ubuntu-latest` runners (a real Docker daemon, unlike some local dev venvs) lets the Docker-gated smoke tests (`test_066_dockerfile.py`, `test_069_container_smoke.py`, `test_070_acceptance_stage9.py`) attempt a real `docker build` instead of skipping — observed failing with `failed to do request: Head "https://registry-1.docker.io/v2/library/python/manifests/3.11-slim": dial tcp ...: i/o timeout` (Docker Hub anonymous-pull rate-limiting/network flakiness, a known GitHub Actions issue, not a numpy-1.26.4 compatibility problem — the same run reported 3851 passed, only the Docker-dependent tests errored). Since `test-numpy-majors` exists specifically to check numpy-major agnosticism, its exposure to Docker-registry flakiness is pure incidental risk with no verification value (the existing `verify-environment-gated` job already owns real Docker verification) and now doubles that flakiness surface (once per numpy leg, on every push). A future item should scope `test-numpy-majors` to skip/deselect the Docker- and PyRadiomics-gated test modules (e.g. `--ignore` or `-m "not docker"` if such a marker exists) so it only re-runs what's actually numpy-sensitive *(item 095, 2026-07-26)* → **Stage 26** candidate **D5**
- [x] framework — `.claude/agents/validator.md`'s foreground-only instruction ("Run it synchronously in the foreground — never as a background task") is scoped to the standalone `pytest` invocation and does not name `python .aide/scripts/aide.py merge NNN`, which — under `git.mode = "auto-merge"` — re-runs the **entire suite again** as its own pre-merge gate and takes the same ~6 minutes. Across queue-013 (5 items), validator sub-agents repeatedly deferred one or both of these long-running commands to a background task and ended their own turn early with a placeholder ("I'll wait for the notification"), leaving the orchestrator with no verdict and no reliable way to know when the real result would arrive — happened on item 093 (twice — the initial pytest run, then `merge` even after a nudge fixed the first one) and item 096 (`merge` alone, pytest itself had run fine). Worked around per-item by adding an explicit "run every command as a plain foreground Bash call, do not background pytest or merge" line to every validator prompt from item 095 onward, which reliably fixed it — but that is a per-invocation patch, not a framework fix. The durable fix belongs in `aide-loop`'s `adapters/claude/agents/validator.md` (materialises here via `.claude/agents/validator.md`, not hand-editable in this repo per `CLAUDE.md`): broaden the existing foreground-only instruction to explicitly name `aide merge` as another command that must run synchronously in the foreground, not only the standalone `pytest` call *(2026-07-26)* → fixed in aide-loop PR #21, shipped in engine 1.4.2 and installed here: the rule now names `aide merge` explicitly at both the point it's introduced and the point it's invoked
- [x] knowledge — partial progress on the `command_hygiene_guard.py`/`local_path` entry above (still open): while fixing the `_item_dependencies`/validator bugs (aide-loop PR #21, engine 1.4.2), the narrower half of that entry's problem got fixed as a side effect — `[framework] local_path` now lives in the personal, gitignored `.aide/loop/loop.local.toml` instead of the shared, committed `aide.toml` (flagged directly by the user, who correctly objected to a machine-specific path landing in a committed file when an agent was about to add it there). That is **not** the entry's main ask, though: the guard's `git -C` exception still only ever covers the one declared framework-repo path, not the general case of "the session has additional working directories it may legitimately need to `git add`/`commit` into" (e.g. a sibling programme repo). Widening the exception generally (declared additional working directories, or a new `[agent] extra_repos`-style allow-list) is a deliberately bigger, security-relevant change and was not attempted here — left for the original entry to still track *(2026-07-26)* → the entry's OTHER specific complaint — "the current shape blocks the honest spelling while leaving `--git-dir`/`--work-tree` open" — is now separately closed: a second aide-loop#21 commit made rule 1 recognise all four repo-override forms (`-C`, `--git-dir`, `--work-tree`, `GIT_DIR=`/`GIT_WORK_TREE=`), checked against the same declared `local_path` (`--git-dir`/`GIT_DIR=` additionally accept the conventional `<path>/.git` value). The broader "arbitrary sibling repos" ask is unaffected and still open → recorded; the parent 2026-07-25 entry stays open for the general "declared additional working directories" ask
- [x] knowledge — `scripts/aide_status_report.py`'s hand-maintained `FEATURE_CATALOG` (components group, lines ~877-893) documents exactly the five pre-098 `ComponentsInfo` fields; item 098 adds four (`stray_component_count`, `stray_component_sizes`, `stray_volume_mm3`, `stray_volume_fraction`) and deliberately does not update the table, because Stage 19 replaces the whole hand-maintained catalogue with a generated one plus a CI drift test. Worth confirming Stage 19's drift test actually catches this specific four-entry gap when it lands — it is a concrete, already-known instance of the drift that stage exists to close *(item 098, 2026-07-26)* → resolved by Stage 19: item 103 deleted the hand-maintained `FEATURE_CATALOG` / `UNWIRED_EXTRACTORS` outright (`scripts/aide_status_report.py` now renders the generated artifact), and all four item-098 stray fields appear in `docs/aide/feature_catalogue.generated.{json,md}` — verified 2026-08-11. The drift class this entry tracked no longer has a place to hide
- [x] gap — the `fuse` perturbation operator (`synth/component_shape.py:209`, registered, tested, and documented as driving "the fragmentation-kind finding on the surviving label (§6 mode 2)") has **no corpus case**: `CASE_RECIPE` (`synth/corpus.py:114-172`) maps §6 mode 2 to `fragment` alone, so the *fused* half of "over-/under-segmentation (fused / fragmented)" is generated by nothing and asserted by nothing end-to-end. Concretely this means one of the ten registered operators never reaches the nine committed goldens, and a mode-2 severity ladder built only from `fragment(n_pieces=…)` measures under-segmentation while claiming to cover the mode. Cheap to close — `fuse` takes the same `(target_label, neighbour_label)` adjacent-pair selector `force_overlap` already uses, so a tenth recipe entry plus a regenerated manifest/golden would do it. Related to but narrower than the existing "synthetic corpus overstates coverage" entry above, which counts unfired *rules*, not ungenerated *operators* *(item 099, 2026-07-26)* → **Stage 20** (per-rule corpus-exercise reporting must count *operators* too, not only rules) and **Stage 21** (the missing `fuse` recipe entry lands with the rung-2 corpus rather than being bolted onto the rung-1 one)
- [x] framework — item 099's `test_ac25_committed_goldens_byte_identical_to_pre_099_state` hardcoded `Path("/mnt/data/spine/codes/SegFACET/tests/corpus/golden")` — the authoring sandbox's own absolute filesystem path — instead of resolving relative to the test file (the neighboring `_SEGFACET_SRC`-based AC25 checks in the same file did this correctly). Because that exact path happens to exist on this development sandbox (it *is* this project's checkout location), the test silently "passed" on every local agent run — builder's own check, both validator rounds, even a from-scratch `git clone` + fresh-venv repro run in a *different* directory, since `Path("/mnt/data/…")` is absolute and ignores where the test process actually runs from — while `glob()` returned nothing on every real CI runner (a different filesystem entirely), collapsing the hash to SHA-256-of-empty-input and failing all four CI legs (ubuntu/windows/numpy-1.26/numpy-2.0) though the environment-gated job and every local validation stayed green. Not caught by the merge cycle at all — discovered only because the user checked GitHub Actions directly and reported the exact `AssertionError` value. **The structural risk**: no validator step in this loop ever checks CI status or diffs against a genuinely different checkout location, so a test with a sandbox-specific absolute path is invisible to every gate this loop runs, indefinitely. Fixed directly on `main` (commit `cd11727`, bypassing the claim/branch/validate cycle as a narrowly-scoped one-line test-only hotfix, confirmed with the user first) rather than through a full item, since the bug was fully diagnosed and the fix trivial to verify. A durable fix belongs in the framework: `.claude/agents/test-writer.md` and/or `validator.md` should warn against/detect hardcoded absolute paths in test files (e.g. a grep-based lint for the repo's own working-directory path appearing literally in `tests/**`), and/or `/aide-run-item`'s validate step should recommend checking real CI status once a push exists, not just the local suite *(item 099, 2026-07-26)*
- [x] gap — §6 mode 7's magnitude is structurally capped at **1** by the label convention, so vision §6.7's own example ("L1 → T12 → L2 → L5", two rank descents) is not representable by any synthetic case FACET can build today. `features/relationships.py:96-100` counts rank descents walking present labels in ascending **integer** order, and under the TPTBox default (item 093) `rank(v) == v - 1` for every value 1-24 — so no relabel inside that block can produce a descent at all. Exactly one value in the whole vertebra range sits below its integer position (**28 = T13**, rank 19), and because 28 exceeds every lumbar value it always sorts last, contributing at most one descent. Consequences: `sequence_break` can only ever produce a 2-rung absent/present ladder (item 100 records this openly), and the `sequence` rule has never been exercised against a *multi*-descent sequence — the very shape §6.7 names. Closing it needs a perturbation that relabels across anatomical groups (e.g. a thoracic value onto a lumbar body mid-span), which `build_clean_spine`'s contiguous-span guard currently prevents by construction; worth a Stage 20/21 look rather than a fix inside a metric item *(item 100, 2026-07-26)* → **Stage 20**, which now records mode 7's representability cap explicitly as a corpus limitation rather than a detection gap; the cross-anatomical-group relabel perturbation that would lift it is **Stage 21**'s
- [x] gap — `eval/per_mode.py::compute_per_mode_metrics` (item 099) always makes its own `compute_overlap` call and offers no way to pass a precomputed `OverlapResult`, so every caller that already holds one pays a second full pass over the label map. That is exactly the shape of the Stage-7 harness: `eval/harness.py::evaluate_case` computes `overlap = compute_overlap(candidate_arr, gt_arr, gt_spacing)` for its own `overlap` field (`harness.py:407`), and item 101's `per_mode=True` hook then calls `compute_per_mode_metrics` on the *same* three inputs, duplicating the pass per case. Item 101 accepts the cost rather than widening the item-099 API (its scope fence pins `per_mode.py` byte-identical, and items 099/100 froze that signature), but the fix is small and local — an optional `overlap_result=None` keyword that short-circuits the internal call when supplied — and would halve the overlap work of any cohort-scale per-mode run. Worth doing when `per_mode.py` is next legitimately open *(item 101, 2026-07-26)* → **Stage 26** candidate **D4** (optional `overlap_result=` short-circuit)
- [x] defect — item 101's `normalised_delta = delta / max(|value_a-baseline|, |value_b-baseline|)` formula saturates to exactly `±1.0` whenever `value_a == baseline`, regardless of the real magnitude of the move — 7 of 8 `PER_MODE_METRIC_SPECS` baselines are `0.0`, so any comparison where the "off"/clean run sits exactly at baseline for two or more modes produces a tie at `1.0` across all of them, resolved only by AC13's lowest-mode tie-break. This is not confined to the flagged AC16 corpus fixture: the hand-built `test_ac13_attributed_mode_is_the_largest_normalised_move` (modes 1 vs 3, both starting at baseline 0.0) hits the identical trap and fails the same way (`attributed_mode == 1`, not the intended `3`), despite being deliberately constructed with different-magnitude deltas (0.1 vs 0.9) to demonstrate "largest move" — the formula is blind to that difference once `value_a` sits exactly on baseline. Any future per-mode-magnitude test/fixture that wants to demonstrate differential attribution must give `value_a` a nonzero offset from baseline on every compared mode, not just the corpus-fixture case validator flagged first *(item 101, 2026-07-27)* → **Stage 26** candidate **D1** (with the 2026-07-27 AC16 entry above)
- [x] framework — the AC27-style byte-identity scope fence (`_PRE_NNN_HASHES` pinning a file's SHA-256 in one item's own test module) breaks by design the moment a *later* item is legitimately authorized to extend that same file: item 101's AC27 explicitly permits editing `cli.py` and `eval/eval_report_schema_v0.json`, which are exactly the files items 099 (`test_099_per_mode_metrics.py::test_ac25_named_file_byte_identical_to_pre_099_state[cli.py]`) and 100 (`test_100_severity_ladder.py::test_ac26_named_file_byte_identical_to_pre_100_state[cli.py]` and `[eval/eval_report_schema_v0.json]`) pinned as "untouched forever." Item 101's own Testing Strategy asserted these older tests would "stay green unmodified," which is structurally impossible given its own authorized file list — a spec self-contradiction that only a live pytest run surfaces, not spec review. Needs a documented convention (in `.aide/conventions.md` or the scope-fence template) for updating a superseded item's pinned hash when a later item is deliberately given permission to touch that same shared file (`cli.py`, bundled JSON schemas), so it doesn't silently recur on every subsequent item that legitimately extends the CLI or a schema *(item 101, 2026-07-27)* → **(pending handover, 2026-08-11)** — to be filed against `dadrobny/aide-loop` ([framework] repo); not filed at triage time, no `gh` on the triage host → **resolved: aide-loop 1.6.0** — `conventions.md` demotes the byte-hash fence to a fallback behind `## Authorised paths`, and states the re-pinning rule this entry asked for (update the earlier constant in the same commit, naming the authorising item) plus the audit-by-shape rule
- [x] defect — `progress.md`'s Stage 17 acceptance list renders its fourth box as `- [x] A real segmenter output round-trips with correct level names.` while that same line's own annotation opens `*(Not ticked: no real SPINEPS output is available…)*` — the checkbox and the prose directly contradict each other, so a reader (or an `aide check` rollup) gets the opposite answer depending on which half it reads. Item 097's Decisions log makes clear the *intent* was unmet, so either the box should be `- [ ]` or the annotation should be reworded to explain why a met-in-mechanics/unmet-in-reality claim is ticked. Worth checking whether `aide check` requires every box ticked before a stage may be ✅ — if it does, that pressure is itself the bug, and the honest fix is a third state rather than a false tick *(item 102, 2026-07-27)* → **Stage 26** candidate **D7**. Not fixed inline here because unticking the box may change what `aide check`'s rollup reports for an already-✅ stage, and choosing between untick / reword / third-state is a maintainer call
- [x] gap — §6 mode 6's specificity shortfall is an artefact of mode **1**'s ladder, not of mode 6's metric. `RECORDED_MARGINS[6] == 0.3585` because `crop_at_border`'s ladder drives `unanchored_foreground_fraction` through 2.79× more of its own full swing than `displace`'s own ladder does — and the reason `displace`'s swing is so small is that item 100's mode-1 ladder is FOV-capped at ~19.8 mm `displacement_mm` on the 5-level `build_clean_spine()` base (`KNOWN_CROSS_MODE_COUPLINGS`'s own recorded cause). So the fix is not to weaken the bar or to re-pick mode 6's metric but to give the **mode-1 ladder a wider base** (a larger FOV, or a base with headroom for a 30-40 mm displacement) so its own metric's full swing is set by the perturbation rather than by the fixture's walls, after which mode 6's response ratio should fall below 1.0 on its own. Cheap to test — the harness already takes `base=` (`run_severity_harness(base=…)`) — and it would turn the one mode that fails Stage 18's G2 "comparatively insensitive to the others" clause into a pass. Stage 20 territory; item 102 records the shortfall but must not fix it *(item 102, 2026-07-27)* → **Stage 20**: widen item 100's mode-1 ladder base so its own metric's full swing is set by the perturbation rather than by the fixture's FOV walls, after which mode 6's response ratio should fall below 1.0 without touching mode 6
- [x] gap — three of the four stray-component fields item 098 promoted to first-class (`stray_component_count`, `stray_volume_mm3`, `stray_volume_fraction`) are read by **nothing** — not by any of the 10 registered rules, not by `eval/per_mode.py`, not by `human_report.render_feature_table`, and not by the reference/eval feature vocabularies (`MORPHOLOGY_FEATURES`, `INGESTED_FEATURES`, `TRACKED_FEATURES`). Only `stray_component_sizes` is consumed, by `heuristics/fragmentation.py`. Measured while prototyping item 103's access tracer over the nine committed corpus records: of the 67 schema-level leaf paths in a realised `extract_feature_record`, a dynamic rule trace attributes 24, adding a static AST scan of `heuristics/*.py` reaches 27, adding traced non-rule consumers 32, and adding the declared feature vocabularies 33 — leaving **34 leaf paths read by nothing at all**, including every `bbox_voxel`/`bbox_physical` corner, `centroid_voxel`, all of `stage3.spacing_consistency.*` and all of `stage3.curvature.*`. This is the honest input Stage 19's keep/retune/retire review and Stage 20's traceability matrix need, and it is a strictly larger finding than the existing item-098 entry above (which is about the hand-maintained table being out of date, not about the fields being unconsumed) *(item 103, 2026-07-27)* → **Stage 20**'s matrix input, **reframed** by the 2026-08-11 Stage-20 traceability clarification (`roadmap.md`): a leaf path read by nothing is legitimate inventory (`unwired`), not a defect — the feature pool is deliberately larger than what today's rules consume. What the matrix must prove is mode→rule coverage and rule→mode purpose, so the 34 unread paths are context, not the finding
- [x] framework — authoring a spec for an item whose dependency is *specified but not yet built* forces the spec author to code defensively around whatever the dependency's own spec left unpinned: item 103 pins `iter_leaf_paths`/`iter_driver_records`/strict-mode behaviour precisely, but never fixes `catalogue_to_dict`'s JSON layout or whether `iter_driver_records()` includes the augmented-tier drivers, so item 104 had to absorb both as a tolerant reader plus a hand-back clause instead of a straight assertion. `/aide-spec-queue` batches exactly this situation (N specs authored on one branch before any of them is built), so a convention for spec-time interface pinning between two unbuilt items in the same queue — e.g. the earlier item's spec must enumerate the serialised shape its downstream consumers read — would remove the guesswork rather than pushing it into every consumer *(item 104, 2026-07-27)* → **(pending handover, 2026-08-11)** — to be filed against `dadrobny/aide-loop` ([framework] repo); not filed at triage time, no `gh` on the triage host → **resolved: aide-loop 1.6.0** — `conventions.md` §5 and `/aide-spec-queue` now make interface pinning run both ways: the *producing* spec must enumerate the **serialised** shape its consumers read (JSON layout, which records a walk yields, what strict mode rejects), not only its API
- [x] defect — `tests/golden/016_features_report.json` and `tests/golden/022_stage3_report.json` are the only committed byte-reproducible text fixtures in the repo **absent from `.gitattributes`** — every other one (`tests/corpus/manifest.json`, `tests/corpus/golden/*.json`, `tests/corpus/intensity/manifest.json`, `tests/corpus/094_pre_migration_snapshot.json`, `src/segfacet/**/*.py`, `src/segfacet/**/*.json`) is pinned `text eol=lf`. It is latent rather than live only because both consumers compare with `read_text()` (universal-newline translation silently normalises a CRLF checkout back to `\n`); the moment any future item switches either comparison to `read_bytes()` — which is exactly what a scope-fence test does, and this repo has now shipped six of them — it reproduces the Windows-CI-only failure documented three times above (items 099-101). One-line fix: add `tests/golden/*.json text eol=lf`. Found while surveying every committed exact-match fixture for item 105's golden decision table; not fixed there because that item's scope fence forbids editing `.gitattributes` *(item 105, 2026-07-27)* → **Stage 26** candidate **D3** (with the entry below)
- [x] defect — `tests/test_022_stage3_serialisation.py::test_ac8_golden_snapshot` **writes its own golden and `pytest.skip`s when the file is absent** (lines 786-789), so deleting `tests/golden/022_stage3_report.json` makes the check pass rather than fail — the opposite of `synth/golden.py::read_golden_text`, whose docstring states outright that "a missing golden must fail loudly, never silently pass", and of `tests/test_042_golden_determinism.py::test_ac14_missing_golden_fails_loudly`, which pins that behaviour for the corpus goldens. A CI runner with a stale checkout, a `.gitignore` accident, or a future item that retires the file would all be reported as green. The sibling `test_016_features_json.py::test_ac5_golden_snapshot` has no such branch and fails correctly, so the fix is to delete the self-healing branch and let `read_text` raise. Found during item 105's survey of every committed exact-match fixture; not fixed there because that item may not edit any test that consumes a golden *(item 105, 2026-07-27)* → **Stage 26** candidate **D3**. Note both `tests/golden/016_*.json` and `022_*.json` are dispositioned **retire** in item 105's decision table, so D3 is "fix the self-healing branch, or retire the golden" — whichever Stage 21 reaches first
- [x] gap — Stage 19's G8 acceptance sentence (`roadmap.md`, mirrored in `progress.md`: *"Every feature carries a status and a named failure mode, or is marked `unwired`"*) has a **third state it does not name**, and item 103's own design guarantees that state will be occupied: its AC15 defines an entry consumed by a rule with no §6 mode mapping (`bounds`, `intensity`, `reference_delta`, `intensity_reference_delta`) and no item-099 anchor as `status == "keep"`, `failure_modes == ()`, `mode_evidence == ("rule_unmapped",)` — statused, not `unwired`, and naming no mode. So the criterion is literally unsatisfiable-as-written by the mechanism the same stage ships, and item 106 has to tick the box with a counted shortfall rather than a clean claim. The honest close is either a roadmap wording fix (a fourth permitted state, "statused but mode-unmapped, with the unmapped rule named") or Stage 20's traceability matrix mapping those four rules to modes — which is the real fix, since a rule that consumes features but targets no catalogued failure mode is itself the finding *(item 106, 2026-07-27)* → closed by the 2026-08-11 roadmap amendment: Stage 19's G8 sentence now names the fourth permitted state (statused but mode-unmapped, with the unmapped rule named), and **Stage 20** owns the real close — mapping `bounds` / `intensity` / `reference_delta` / `intensity_reference_delta` to §6 modes, or recording that they target none
- [x] framework — the loop has **no mechanism for a mid-queue human approval gate**, so queue-015 encodes one by hand and fragilely: item 105 must obtain explicit maintainer approval at its Validation step and tick a `progress.md` checkbox, and item 106 must halt if that checkbox is unticked — a protocol that exists only as prose in two item specs and in a biconditional pytest item 106 invents for the purpose. `/aide-run-queue` and `/aide-run-item` are documented as pausing only for PRs and major structural changes, so an unattended run reaching item 105 either blocks on a prompt nobody is there to answer or (worse) proceeds; and `aide progress set` handles only deliverable rows (`in-progress|done`), never acceptance checkboxes, so the attestation itself is a hand edit no CLI validates. Worth a first-class notion — a spec-declarable "human gate" that the orchestrator stops at and that `aide check` can report as outstanding — rather than each stage that needs a steering checkpoint re-deriving one *(item 106, 2026-07-27)* → **(pending handover, 2026-08-11)** — to be filed against `dadrobny/aide-loop` ([framework] repo); not filed at triage time, no `gh` on the triage host → **resolved: aide-loop 1.13.0** — human gates are first-class: a `## Human gates` table in `progress.md`, reach declared per gate (item numbers / `stage N` / `all`, never a queue), `aide gate (list|approve|decline)` as the attestation, `aide claim` refusing a blocked item *by name*, `aide check`/`aide status` reporting outstanding gates, and no agent permitted to resolve one
- [x] defect — item 104's AC18 ("at least one committed entry has `origin == \"augmented\"`") cannot pass against the real committed tree: `src/segfacet/catalogue.py::build_catalogue` constructs every `CatalogueEntry` with a single hardcoded `origin="record"` (line ~865, the only construction site) -- there is no code path that ever assigns `"augmented"`. The module's own docstring ("Two tiers, one `origin`") says this is deliberate: both driver tiers "flow through the *same* `iter_leaf_paths` walk and therefore the same `origin == \"record\"` classification". Item 104's spec Assumptions section reads this docstring backwards, inferring a real second origin value (`"augmented"`) exists for `image_features.*`/`reference_delta.*` paths and pins AC18 against it; confirmed empirically against the committed `docs/aide/feature_catalogue.generated.json` -- all 111 entries, including every `image_features.*`/`reference_delta.*` path, carry `"origin": "record"`. Item 104 explicitly forbids any production change (AC22), so this AC cannot be made to pass without either amending item 103's `build_catalogue` (out of this item's scope) or correcting item 104's spec to drop the augmented-origin assumption. The test module (`tests/test_104_feature_catalogue_drift.py`) was written faithfully to the spec as given, so `test_ac18_at_least_one_augmented_entry_exists` is expected to fail until this is resolved *(item 104, 2026-07-28)* → resolved during execution: item 104's AC18 was rebuilt around a *synthetic* augmented entry (`test_ac18_synthetic_augmented_entry_outside_coverage_is_exempted` plus `test_ac18_augmented_exemption_never_covers_a_record_tier_path`) rather than asserting a real augmented-origin entry exists; `build_catalogue`'s single `origin="record"` is unchanged and correct. Verified 2026-08-11
- [x] framework — a *third* instance of the self-contradicting byte-hash scope fence is now predictable **before either item executes**, which is new: item 105's AC14 pins digests over the whole `src/segfacet/**` tree, and the 2026-07-27 maintainer amendment authorises item 106 — the very next item, same queue, same stage — to edit `src/segfacet/feature_docs.py`'s `STATUS_OVERRIDES`. Item 104's own Decisions section already predicted this collision by name (*"exactly what item 105/106 will do to `feature_docs.py`'s `STATUS_OVERRIDES`"*) and the standing entry above describes the pattern generically, yet item 105's spec still shipped a whole-tree fence, because nothing in the loop cross-checks one spec's pinned-file set against a *sibling* spec's authorised-edit list. Two cheap durable fixes, either of which would have caught it at spec-authoring time rather than at the first red pytest: (a) `/aide-spec-queue` (or `aide check`) diffing every spec's "may edit" list against every other spec's "pinned byte-identical" list within the same queue and flagging overlaps; (b) a scope-fence convention that fences are always *exclusionary* (`src/segfacet/**` minus paths named by any later item) or per-file rather than whole-tree. Item 106's spec works around it with a narrow commented re-pin (its AC23 item 7, following `test_100_severity_ladder.py:947-949`'s precedent), but that is a per-item patch of a recurring structural gap *(item 106, 2026-07-27)* → **(pending handover, 2026-08-11)** — to be filed against `dadrobny/aide-loop` ([framework] repo); not filed at triage time, no `gh` on the triage host → **resolved: aide-loop 1.9.0** — this entry's fix (a) shipped as `aide check --queue NNN`, which flags one item's **May change** overlapping another's **Asserts against** as an error, plus same-file overlaps and dependency-graph cycles; `--report` writes the findings as JSON for the 1.10.0 `spec-reviewer` agent
- [x] gap — `reference_default.json`'s 21-feature per-level (L1-L5) distribution is built entirely from a 5-subject **synthetic** cohort (`reference/artifact.py::_DEFAULT_COHORT_RECIPE` → `build_clean_spine` + `paint_clean_scan(seed=0)`, no real imaging or GT) and is not the CLI's runtime default (`bundled_production_reference()`/`reference_verse_v1.json`, 80 real VerSe19 subjects, has been the default since item 090) — kept today only as a fixed fallback/synthetic-regression fixture, per the maintainer during item 105's golden-file review (2026-07-28). Two follow-ups the maintainer wants tracked, not actioned now: (1) **consistency risk** — nothing currently checks that this synthetic baseline's *feature ranges* stay plausible relative to `reference_verse_v1.json`'s real distributions, so it could silently drift into physically-implausible territory (e.g. vertebra-size ranges) without any test catching it; worth a bounds/sanity check once Stage 21's real-GT work lands, or periodically during development. (2) **generation-method question** — whether a 5-subject synthetic cohort is the right way to build this fallback at all, versus deriving it from theoretical/prior-knowledge value ranges (e.g. published anatomical size ranges) instead of synthesized geometry, and whether a toolkit exists for generating more realistic synthetic test data generally; worth investigating but explicitly not now — "might not fit anywhere currently" per the maintainer, flagged for whenever reference-artifact generation is next legitimately open (Stage 21 candidate) *(item 105, 2026-07-28)* → **Stage 21** for (1), the feature-range sanity check against `reference_verse_v1.json`; the roadmap's new **Backlog** section for (2), the generation-method question, which stays explicitly not-now
- [x] gap — §6 mode 8 (overlap)'s pipeline-blindness (already tracked in `roadmap.md`'s Stage 20 section as one of three reachability-hole modes, alongside 1/4) has a root cause worth stating explicitly, surfaced during item 106's per-feature steering review of the `overlaps[]` group: with **single-channel** integer-label segmentation files (this repo's only supported input format today), two labels physically cannot share a voxel — a voxel holds exactly one integer value — so `features.overlap.detect_overlaps` can only ever populate `overlaps[]` on a label map deliberately corrupted to violate that invariant (the corpus's `force_overlap` synthetic perturbation does this by construction; no real segmenter output ever could). The `overlap` rule and all six of its consumed fields are fully wired and correctly implemented — the gap is not in the code, it is that the feature the mode targets is structurally unobservable in the current input format. The maintainer notes this becomes meaningfully testable/relevant only if **multichannel (probabilistic) segmentation** input is ever supported, which is not on the current roadmap. Worth citing when Stage 20 records mode 8's reachability hole, so the "not both silent" requirement (`roadmap.md` Stage 20) states the mechanism, not just the symptom *(item 106, 2026-07-28)* → **Stage 20**, whose reachability-hole deliverable now states the *mechanism* per mode rather than only the symptom: mode 8 is unobservable in single-channel integer-label input by construction, and becomes testable only if multichannel / probabilistic segmentation input is ever supported
- [x] gap — item 106's full per-feature steering review (all 111 catalogue entries walked with the maintainer, 2026-07-28) surfaced a single recurring architectural theme across roughly two-thirds of the entries touched, worth a dedicated pass rather than being left as ~70 scattered per-field `STATUS_OVERRIDES` rationales: **the feature schema's organisation is ad hoc (grouped by which extractor module happened to compute a field) rather than by a coherent taxonomy.** The maintainer's own framing: group by **scope** (`per_label` / `per_neighbour-pair` / `per_scan`-or-case-level) crossed with **kind** (shape/geometry, intensity, label-identity, etc.), rather than today's module-provenance grouping. Concrete instances already recorded as individual retunes feed directly into this: (a) `label`/`level_name` identity fields are independently duplicated across `stage3.per_label_offsets[]`, `stage3.per_label_orientations[]`, `image_features.per_label`, and `reference_delta.{label}` — four copies of the same two fields instead of one; (b) the `stage3.*` and `image_features.*` blocks sit as separate top-level containers instead of nesting under the existing `per_label.{label}.*` structure that already holds geometry/components/centroid; (c) axis-aligned bbox/extent fields (Group 1) and `principal_axis` (Group 7) are flagged for replacement by measurements in a **vertebra coordinate system (VCS)** — itself undefined — which is exactly a "kind" reclassification (image-axis-relative → anatomy-relative shape); (d) the reference-delta machinery is flagged to generalise from one hardcoded tracked feature (`physical_volume_mm3`) to any requested feature (Group 11), which only makes sense once features are organised well enough to select a "feature set" from. Recommend this become its own roadmap-level item — a schema redesign, not a per-field retune — rather than being executed piecemeal as each individual override eventually gets picked up *(item 106, 2026-07-28)* → **Stage 27 — Feature Schema Taxonomy & Coordinate System** (`roadmap.md`), a new roadmap-level stage, exactly as the maintainer's framing asked — a schema redesign rather than ~70 piecemeal per-field retunes
- [x] defect — `progress.md`'s Stage 3 claim for item 024 (*"Local vertebra neighbourhood comparison ... flags isolated anatomical outliers"*, ✅) does not hold: `src/segfacet/features/neighbourhood.py` implements the sliding-window (±N) deviation-score computation in full, but it is **never wired into the pipeline** — absent from `pipeline.py::extract_feature_record`, from `feature_report.py`'s block assembly, and from every one of the 10 registered rules (confirmed by grep across `pipeline.py`, `feature_report.py`, `heuristics/*.py`: the only other hit anywhere in `src/segfacet/` is a docstring convention-reference in `features/intensity.py`, not an integration). This is why it never appeared in item 103's 111-entry feature catalogue — it isn't part of the realised record schema at all, not merely `unwired` in the catalogue's sense. Found while answering the maintainer's question, during item 106's steering review (2026-07-28), about whether a neighbourhood-relative comparison concept was already covered by the roadmap — it is *designed and coded* but not *reachable*, so no case's report or verdict has ever been influenced by it despite the ✅. Same class of issue as the already-logged Stage 17 progress.md checkbox/prose contradiction (item 102) — a claim not backed by observable pipeline behaviour. Fixing this (wiring `neighbourhood.py` into the record, deciding whether it needs a consuming rule, and reconciling its mean/std-based deviation score with the fuller percentile-based `robust_z`/`percentile_rank`/`out_of_range` machinery item 106's Group 11 retune already flags for generalisation) is real, undone work — not this item's to do *(item 106, 2026-07-28)* → **Stage 26** candidate **D8** (wire `features/neighbourhood.py` into the record, or retire it). `progress.md`'s Stage 3 bullet annotated 2026-08-11; the ✅ was left in place deliberately, since flipping item 024's status would reopen queue-002 and change what `claim_scope = "live-queue"` resolves to — that flip is a maintainer call
- [x] knowledge — future-extension idea from the maintainer, confirmed absent from `vision.md`/`roadmap.md`/`progress.md` and from `src/segfacet/reference/` (2026-07-28): a **leave-one-out / counterfactual sensitivity feature** — for a given vertebra, measure how much a scan-level shape metric (e.g. spline smoothness/curvature) changes under a hypothetical modification of that vertebra (dropping its label entirely, or merging it into its largest bordering neighbour), as a way of quantifying that vertebra's structural influence on the whole spine's shape. Distinct from both the existing population reference (`reference_delta`) and the existing-but-unwired local-neighbourhood comparison (item 024, see the defect entry above) — this is a counterfactual/ablation-style measure, not a static comparison against either a population or a neighbourhood snapshot. No existing roadmap stage covers it; closest conceptual neighbours are Stage 20 (rule specificity, not a new feature), Stage 21 (perturbation corpus for calibration, not a counterfactual feature), and the Stage 23 placeholder (multivariate normative model, closest in spirit but framed around threshold scoring, not ablation). Flagged by the maintainer explicitly as "to be considered as a future extension" rather than something to act on now — no roadmap stage currently owns it *(item 106, 2026-07-28)* → the roadmap's new **Backlog — unowned ideas** section, which names it, its closest stage neighbours, and the fact that no stage owns it
- [x] framework — two more instances of the recurring "one item's pinned artifact collides with a later item's legitimate change" pattern (`insights.md` already logs this three times over for items 099-101, then predicted-and-handled once for item 105's AC14 digest vs. item 106's `STATUS_OVERRIDES` edit) surfaced during item 106's actual execution, one anticipated and one not: (1) **anticipated and handled** — item 105's AC14 whole-tree `src/segfacet/**` digest broke exactly as item 106's spec predicted, but investigating it surfaced an *independent* pre-existing defect: `_combined_hash`'s unfiltered `rglob("*")` walk includes `__pycache__/*.pyc` — gitignored, untracked compiled bytecode whose bytes embed source mtimes — so the "pinned" digest was never actually reproducible even on an *unchanged* tree (three different values observed across three clear-cache-then-run cycles). This would have broken on literally any future legitimate edit to `src/segfacet/**`, unrelated to item 106. Fixed directly (not a re-pin alone): `_tracked_files()` now excludes any path with a `__pycache__` component; the corpus-side hash was unaffected (no pycache under `tests/corpus/`), the src-side constant was re-pinned to the new, now-verified-stable digest of the post-override tree, with a comment naming item 106's authorisation, following the `_PRE_100_HASHES` precedent. (2) **not anticipated by any spec** — item 105's AC7 test recomputes each corpus golden's "N/67 leaf paths unwired" evidence *live* against `segfacet.catalogue.build_catalogue()` specifically so it can never go stale/transcribed, which is exactly what then broke it: item 106's override edit reclassified 41 of the 67 record-tier paths from `unwired` to `retune`/`retire`, so the live count fell from 34 to 0 for all nine goldens simultaneously. Item 106's own "existing tests to reconcile" survey named only item 105's AC14 as "genuinely at risk" — this second collision, in a *different* AC of the *same* sibling module, was invisible to that survey because AC14 is a static byte-hash (an obviously fragile pattern by now) while AC7 is a live recomputation (deliberately *not* fragile in the byte-hash sense, yet still coupled to the same underlying catalogue state). Fixed by updating the nine evidence cells' numeric values only (`34/67` → `0/67`) in `docs/aide/golden-decision-table.md`, with a dated explanatory note added above the table (no `disposition`/`rationale`/`replacement guarantee` cell touched) — deliberately outside item 106's own scope fence, done directly as a defect/staleness reconciliation once found, the same way the AC14 hash bug was. **The durable gap both instances demonstrate together**: a "predict the one collision named in the spec" survey is not the same as "prove no sibling-module assertion depends on state this item's authorised edit changes" — the second is the actual invariant that matters, and nothing in this loop checks it mechanically yet (echoes the two existing framework entries above proposing a cross-spec pinned-vs-authorised-edit diff at `/aide-spec-queue`/`aide check` time; this shows the check needs to cover *live-recomputed* assertions too, not just byte-hashes) *(item 106, 2026-07-28)*
- [x] framework — the roadmap can express stage *parallelism* but the queue model cannot represent it, so the hint is silently serialised and every planner re-derives the same apology. `roadmap.md:655-659` states that Stages 19 and 20 "are pure audit — they touch no production behaviour and **should run alongside 17/18**", yet `aide-create-queue` scopes a batch to one cohesive roadmap unit and `aide.toml`'s `claim_scope = "live-queue"` admits exactly one live queue at a time. Consequence: queue-013 (Stage 17) and queue-014 (Stage 18) each had to spend a paragraph explaining why they were *not* honouring that instruction, and a reader comparing roadmap to queues sees a standing contradiction. Either the roadmap needs a concurrency marker the planner actually consumes (a second live queue, or a `parallel-with:` field), or `aide-create-queue`'s stage-boundary rule should say outright that "run alongside" in a roadmap means "queue next, sequentially — independence, not concurrency", so planners stop treating it as a conflict to justify away *(queue-014, 2026-07-26)* → **(pending handover, 2026-08-11)** — to be filed against `dadrobny/aide-loop` ([framework] repo); not filed at triage time, no `gh` on the triage host → **resolved: aide-loop 1.12.1** — vocabulary, not machinery: `conventions.md` names the three senses of "parallel" and states that one queue is live at a time by design; `/aide-create-queue` now tells the planner that "run alongside" means independence, so queue next sequentially *and say nothing about it*
- [x] framework — items 099-101 each introduced a "scope fence" test that hashes committed source files verbatim (`f.read_bytes()`) to prove a later item didn't touch them out of scope (e.g. `test_ac25_named_file_byte_identical_to_pre_099_state`, `test_ac27_named_untouched_file_byte_identical_to_pre_101_state`), but none of the eight newly-hashed files (`cli.py`, `eval/metrics.py`, `eval/per_mode.py`, `eval/severity_ladder.py`, `eval/calibrate.py`, `eval/overlap.py`, `report_schema_v0.json`, `eval/eval_report_schema_v0.json`) or the three hashed packages (`heuristics/**`, `features/**`, `synth/**`) were ever pinned `text eol=lf` in `.gitattributes` — the exact class of bug `CLAUDE.md`'s Gotchas section already documents for *generated fixtures*, just newly hit by plain *source* files. A Windows CI runner's default `core.autocrlf` rewrites their committed LF to CRLF on checkout, so the pinned hash never matches; discovered only because the user pasted the actual Windows job output (13 failing tests spanning all three items' test files) after item 101's merge — no step in this loop (builder, two validator rounds per item, `aide merge`'s own re-test gate) ever runs on a non-Linux checkout, so this was invisible throughout. Separately found one more instance of the *original* item-099 bug class in the same sweep: `tests/corpus/094_pre_migration_snapshot.json`, a plain JSON file sitting directly under `tests/corpus/` that slipped through the existing fixture-specific globs (`tests/corpus/manifest.json`, `tests/corpus/golden/*.json`, `tests/corpus/intensity/manifest.json`) because it matched none of them exactly. Fixed directly on `main` (commit `694d955`) rather than through a full item — verified every affected file's committed blob was already pure-LF (zero `\r` bytes) first, so this is a `.gitattributes`-only fix, and pinned the whole `src/segfacet/**/*.py`/`**/*.json` tree (not just the currently-hashed files) so a future item's new scope-fence test doesn't reopen this on yet another path. **Durable fix still open**: this is the second time a Windows-only CI break from this exact test *pattern* (byte-hashing committed files without an LF pin) has gone undetected through the full spec→test→build→validate→merge cycle and only surfaced from a human reading the Actions tab directly — the underlying risk named in the item-099 entry above (no step in this loop ever checks a non-Linux checkout or real CI status) is unresolved and this is now two independent instances of it *(items 099-101, 2026-07-27)*
- [x] framework — the entry above marking the LF-pin fix (commit `694d955`) "closed" was premature: it fixed only the single-file scope-fence tests (`cli.py`, `eval/metrics.py`, etc., each hashing one file's raw bytes). A second, independent bug in the SAME test pattern survived that fix and kept failing on the real Windows CI runner: `_combined_hash` (and its inline duplicate in `test_099_per_mode_metrics.py`), used by every *package-level* scope-fence test (`heuristics/**`, `features/**`, `synth/**`, and the whole `tests/corpus/` tree), fed `str(f.relative_to(base))` into the hash alongside file bytes — `str()` renders a `Path` with the OS-native separator, so the identical tree hashes differently on Windows (backslash-joined) than on the Linux sandbox the constants were pinned against (forward-slash-joined), independent of line endings entirely. Caught the same way as before: the user pasted the next round of real Windows CI output showing exactly these 9 tests still red after the LF fix landed. Fixed directly on `main` (commit `3e218cd`) by switching all three occurrences to `Path.relative_to(base).as_posix()`, verified to produce byte-identical hashes to the existing pinned constants on this Linux sandbox (no constant needed to change) while being OS-independent. **The durable-fix gap named in the entry above is now demonstrated twice over in one afternoon** — a genuinely non-Linux-checkout-aware step (even a single Windows/`as_posix()`-consciousness check in `test-writer.md`'s guidance for byte-hash-style scope-fence tests) would have caught both of these before either reached `main` *(items 099-101, 2026-07-27)*
- [x] gap — item 106 spec's Testing Strategy "Adversarial / edge cases" for AC18 claims an entry with `status == "unwired"` and non-empty `failure_modes` should make the check "fail (buckets must be disjoint)", but the real committed `docs/aide/feature_catalogue.generated.json` (item 103) already contains exactly that combination legitimately: `stage3.monotonic_consistency.is_monotonic` has `status: "unwired"`, `consuming_rules: []`, `consumers: []`, yet `failure_modes: [4]` (a `MODE_ANCHOR_PATHS` §6-mode anchor is independent of rule/other consumption by item 103's own design). AC18's own bucket-(ii) definition ("unwired — `status == 'unwired'`") doesn't actually require empty `failure_modes` either, so the three buckets stay exhaustive/disjoint on the status axis alone; only the adversarial-case *prose* overstates the constraint. `tests/test_106_stage19_validation.py`'s `_status_partition` was written to the AC's literal three-bucket definition (bucket ii = `status == "unwired"`, full stop), not the stronger prose, since the stronger reading is falsified by the current committed artifact *(item 106, 2026-08-11)* → no action needed: `_status_partition` correctly implements AC18's literal three-bucket definition, and the committed artifact falsifies the stronger prose. Recorded here as the standing reading of AC18; item 106's spec stays as shipped (merged item specs are historical records)
- [x] defect — item 106's `docs/aide/items/106-validate-stage19.md` "### Stage-19 steering review" transcript (Decisions & Trade-offs) does not verbatim-transcribe every `STATUS_OVERRIDES` key as AC25 requires: grouped bullets abbreviate all-but-the-first key in a shared-prefix group to a leading-dot suffix (e.g. `per_label.{label}.geometry.bbox_physical.x_max`, then `.bbox_physical.x_min`, `.bbox_physical.y_max`, ... — never the full dotted path again), so `test_ac25_steering_review_heading_present_and_honest`'s literal `key in section` substring check fails for 49 of the 74 real override keys once the section-boundary bug (finding the AC25 prose mention instead of the real heading) is fixed. Discovered while fixing two other pre-existing test bugs in `tests/test_106_stage19_validation.py` on branch `aide/specs-queue-015` (2026-08-11); left unfixed since it requires editing either the spec transcript (out of scope: 106's spec is off-limits to a test-only task) or AC25's literal-match test semantics (not one of the two authorized bugs) — needs a maintainer call on which side is wrong *(item 106, 2026-08-11)* → fixed directly by the orchestrator (root cause: the orchestrator's own instructions to the builder allowed the grouped-abbreviation style without flagging it would break AC25's literal check): added an unabbreviated appendix listing all 74 override keys in full, verbatim form directly under the narrative transcript in `docs/aide/items/106-validate-stage19.md` — the grouped prose stays for readability, the appendix satisfies AC25's literal per-key requirement. `tests/test_106_stage19_validation.py` is now 70/70 green *(2026-08-11)*
- [x] framework — `insights.md`'s header comment contradicts the triage workflow written six lines above it: the closing rule reads "Append-only: never rewrite, reorder, or delete existing lines", while the triage paragraph directly above instructs the triager to tick the checkbox in place and append a "→ where it landed" pointer — both of which rewrite the captured line. Every triage pass therefore has to violate the stated rule to follow the stated procedure. The fix is a wording change in the framework's own `core/templates/insights.md` (loosen the sentence to permit exactly those two triage edits — tick and append-pointer — while still prohibiting any other rewrite of a captured line), not a local edit: `.aide/templates/**` is installer-generated here and a hand-edit is silently overwritten on the next `install.py --update`, and `docs/aide/insights.md`'s header is a verbatim copy of that template, so editing either in-tree only creates drift. Both were reverted for that reason; this entry is the durable record *(2026-08-12)* → **(pending handover, 2026-08-12)** — to be filed against `dadrobny/aide-loop` ([framework] repo) as a template correction → aide-loop issue #22, fixed in PR #32 (engine 1.5.0, installed here 2026-08-17): the template and `conventions.md` §1 now permit exactly the two triage edits — ticking the box in place and appending this pointer — and prohibit any other rewrite, reorder or deletion. This entry is the first one triaged under the corrected rule
- [x] framework — `aide check` reports the framework's own documented queue-branch name as a stale item claim: `/aide-create-queue`'s hand-off and `/aide-run-roadmap` both name `aide/queue-NNN` as the branch a queue lands on, but the claim parser reads the trailing integer as an item number, so branch `aide/queue-016` produces *"stale claim branch aide/queue-016: item 016 is already ✅"* — item 016 being a long-completed work item from queue-002, entirely unrelated. The same false positive fires for `aide/specs-queue-015`. Consequence: every queue branch created the documented way emits a spurious warning for as long as it exists, and a real stale claim is that much easier to miss among them. Fix is to exclude branch names matching the queue-branch convention (`aide/queue-NNN`, `aide/specs-queue-NNN`) from claim parsing, or to require a distinct prefix for item claims *(2026-08-12)* → aide-loop issue #22, fixed in PR #32 (engine 1.5.0, installed here 2026-08-17): branch→item resolution is now one anchored helper shared by `check`, `merge`, `sync --item`, `status` and `gc`, and queue/specs-queue branches are recognised positively — the two stale-claim warnings this repo carried for `aide/queue-016` and `aide/specs-queue-015` are gone. The fix also closed a far worse consequence than the warning that surfaced it: because `gc` targets any branch whose item is ✅ independently of `--merged`, `aide gc --yes` would have deleted an in-flight queue branch, local and remote, along with the queue file and item specs living only on it
- [x] defect — item 107 (validated 2026-08-12) deleted `test_099_per_mode_metrics.py::test_ac25_committed_goldens_byte_identical_to_pre_099_state` as an authorised fence removal, but `docs/aide/golden-decision-table.md` still names that exact function in its "asserted by" column for all nine golden-fixture rows, so `test_105_golden_decision_table.py::test_ac6_asserted_by_cells_resolve_to_real_tests` goes red — the same class of collision item 106 hit and fixed directly (see the two `(item 106, 2026-07-28)` entries above), but this time not caught before hand-off to the validator, i.e. the builder did not run the full suite (spec Implementation Step 4 says to). Confirms the golden-decision-table.md "asserted by" column is a durable, easy-to-miss coupling point for any item that deletes or renames a test function it names — worth a standing check (grep the table's function-name cells against the live suite) rather than relying on each item's author to remember the item-106 precedent *(item 107, 2026-08-12)* → verified resolved 2026-08-25: the nine Group-A "asserted by" cells were reconciled by item 107 itself (see `docs/aide/golden-decision-table.md`'s "Asserted by cells reconciled 2026-08-12" note) — the one surviving mention of the deleted function is inside that prose note, not in a cell. The standing check this entry asked for exists and is green: `tests/test_105_golden_decision_table.py::test_ac6_asserted_by_cells_resolve_to_real_tests` resolves every "asserted by" cell against the live suite, so the coupling point is now machine-checked rather than left to each author remembering the item-106 precedent
- [x] knowledge — **project-wide normalisation policy, settled 2026-08-12.** Two rules govern any scaling of a feature or metric. **(1) A normalisation factor must never introduce a supervision dependency.** Anything derived from ground truth — a GT label count, "the levels this scan *should* have", a reference annotation — is supervision, not a feature; scaling by it yields a number that cannot be computed on real segmenter output, which is the setting FACET exists to analyse, and mixes supervision into the feature space. This holds even for metrics whose own `source` is `candidate_vs_gt`: that a metric needs GT does not license its *divisor* to import further GT-derived quantities. **(2) Normalisation is human-reviewed, or it does not happen** — the sole exception being a scaling intrinsic by construction (already dimensionless, or bounded 0..1 with a derivable full swing, where the denominator falls out of the metric's own definition and no judgement is exercised). The default absent review is the raw value. Applied to the per-mode metrics: the fraction-valued ones scale intrinsically; `rogue_island_count` stays raw (a *maximum over per-label entries*, so a scan-level denominator would change the quantity) and `missing_level_count` stays raw (its only natural denominator is GT-derived, barred by rule 1). A small reviewed threshold is the plausible candidate for rogue islands, whose clean expectation is *none* — value TBC, mechanism shipped by item 109 with nothing set. Where no reviewable global constant is defensible at all, the fallback is **neighbourhood-relative** comparison — a vertebra against its own neighbours rather than anything global or supervised — for which item 110's generalised API is the mechanism and Stage 27 the likely owner. Recorded in `roadmap.md`'s Backlog *(item 109, 2026-08-12)* → folded into `roadmap.md`'s Backlog as "Feature and metric normalisation policy" — both rules, the per-mode application (fractions scale intrinsically, `rogue_island_count` and `missing_level_count` stay raw), and the neighbourhood-relative fallback with Stage 27 named as its likely owner. Verified present 2026-08-25
- [x] framework — `aide merge` hard-wires its merge target to `[git] main_branch` (`.aide/scripts/aide.py:1341`, then `git switch <main>` at :1353) and offers no override, so the loop cannot land work anywhere but `main`. A **`--base` flag on `claim` and `merge`** (defaulting to `main_branch`, never removing it as the default) would support the stacked branching structures real work produces. Concrete case, queue-016: the queue file, a roadmap deliverable and all nine item specs live only on `aide/queue-016` and are meant to land as one reviewed PR after the stage validates, so each item must branch off **and merge back into** the queue branch. Half the machinery already does the right thing — `aide claim` runs `git switch -c <branch>` from whatever is checked out (:1322), so claiming from the queue branch branches correctly — it is only the merge target that is fixed. The available workarounds are all poor: repointing `main_branch` in `aide.toml` also repoints `sync`/`gc`/`status` and leaves a trap in a committed, PR-gated file; or the orchestrator merges by hand, which is what queue-016 does, at the cost of overriding `.claude/agents/validator.md`'s documented "run `aide merge`" step once per item and re-implementing that verb's pre-merge suite gate, push and branch cleanup in prose. Note the same fix would let `gc --merged` and `status`'s ahead/behind reporting take a base too *(2026-08-12)* → **resolved: aide-loop 1.8.0** — `--base <ref>` on `claim`, `merge`, `gc`, `status` and `scope`, with `main_branch` still the default everywhere; the claim *records* what it branched off, so `aide merge NNN` returns an item to its queue branch without the flag and the validator's documented merge step is already correct there
- [x] defect — item 108: `tests/test_108_affine_faces.py::test_ac9_singular_affine_deterministic_outcome`'s fixture construction (`nib.Nifti1Image(_small_labelmap(), affine)` with a rank-deficient affine such as a zeroed row) raises `nibabel.spatialimages.HeaderDataError` from nibabel's own `set_qform`/header-sync path at image-construction time, before `compute_label_geometry` is ever called, so the intended AC9 assertion (a clear error or deterministic result from the production function) is never reached -- the test errors out of fixture setup instead. Confirmed on nibabel 5.4.2. Not a `geometry.py` defect: `_face_map_from_affine`'s own singular-affine handling (raises `ValueError`) is unreachable via this fixture and untested; a fixture that builds an already-degenerate `Nifti1Image` without triggering nibabel's internal qform decomposition (e.g. constructing the header separately and assigning `.affine` post-hoc, bypassing `set_qform`) would be needed to actually exercise that path *(item 108, 2026-08-13)* → verified resolved 2026-08-25: `tests/test_108_affine_faces.py::test_ac9_singular_affine_deterministic_outcome` now builds the image with a harmless placeholder affine and assigns the singular one onto `.affine` post-construction — exactly the bypass this entry proposed — so `_face_map_from_affine`'s own degenerate-affine handling is reached and asserted instead of nibabel's constructor raising first. The test's docstring records the reasoning
- [x] defect — item 107's fence retirement was **incomplete, and its own acceptance test cannot detect that**. AC1 was worded as "no `_PRE_099_*`/`_PRE_100_*`/`_PRE_101_*`/`_PRE_103_*`/`_PRE_105_*` constant remains", so the grep searched for a *naming pattern* rather than for the *shape of the assertion*. Three byte-hash scope fences survive under other names, found while item 116 regenerated a generator-derived artifact: `tests/test_093_tptbox_label_convention.py`'s `_PRE_ITEM_REFERENCE_DEFAULT_SHA256` (pins `reference_default.json` "before item 093's edit" — this one actually fired, breaking item 116's legitimate regeneration), `tests/test_098_stray_components.py`'s pinned pre-098 digest of `reference_verse_v1.json`, and `tests/test_102_stage18_validation.py`'s path-to-digest dict asserted at line 668. Item 116 retires the first (AC15); the other two are untouched because nothing this item changed made them fire. **Item 115's AC8 inherits the same flaw** — it asserts "no `_PRE_[0-9]` constant exists anywhere under `tests/`", which would pass while all three of these remain. The audit needs to search by assertion shape (a SHA-256 taken over a committed file's bytes and compared to a literal) rather than by constant name; the intra-run before/after digests in `test_100`/`test_102` must stay, so the discriminator is "compared against a hardcoded constant" versus "compared against a value computed in the same run" *(item 116, 2026-08-12)* → audited 2026-08-25, two of the three named fences are gone and the third is legitimate. `_PRE_ITEM_REFERENCE_DEFAULT_SHA256` was retired by item 116 (AC15) as predicted. What survives in `test_102_stage18_validation.py` is an intra-run before/after digest dict (hashed and re-compared inside one run), which this entry's own discriminator says must stay — so that third claim does not name a fence. The one live residue is `_PRE_098_REFERENCE_VERSE_V1_SHA256`, routed to `roadmap.md`'s "Carried defects" section together with the judgement recorded in the item-115 entry below. The audit-by-assertion-shape this entry called for is what item 115's AC8 implemented
- [x] gap — item 116's spec Validation section says to confirm the mode6_crop_at_border finding names a "cranio-caudal" face, but the corpus's designated case has always (pre- and post-migration, unchanged by this item) cropped toward the "anterior" face -- verified by running `segfacet run` on the regenerated fixture: the border finding correctly reads "touches image face(s): anterior", anatomically sensible and matching perturbation_params.face, just not cranio-caudal. Non-blocking (the actual behaviour and case design are correct; only the spec prose's wording is off) *(item 116, 2026-08-13)* → corrected in place 2026-08-25: `docs/aide/items/116-ras-native-synthetic-corpus.md`'s Validation section now carries a dated correction rather than a rewrite — the designated case crops toward the **anterior** face, matching its own `perturbation_params.face`, and only the spec sentence was ever wrong. The same note retires that section's second stale instruction, a `scripts/check_item_scope.py` invocation deleted by item 117
- [x] gap — item 110's spec authorises edits to `src/segfacet/{neighbourhood.py,pipeline.py,feature_report.py,feature_docs.py}` and the golden/catalogue docs but never lists `src/segfacet/report_schema_v0.json`. Both `#/definitions/features` and `#/definitions/stage3` in that schema declare `additionalProperties: false`, so wiring a new `stage3.per_label_neighbourhood` (or any new stage3 key) key makes every schema-validating report on a >=2-label case fail validation the moment a real case runs through `serialize_report` (e.g. any corpus case via `run_qc`/`build_report_for_case` in test_035/test_042) -- not just new item-110 tests, the existing schema-validation tests break too. The builder needs `report_schema_v0.json` added to the authorised-paths set (or an item-107-style pre-merge fence exemption) before AC8 can land without also breaking unrelated already-green tests *(item 110, 2026-08-13)* → verified resolved 2026-08-25: item 110's spec was amended before implementation — `src/segfacet/report_schema_v0.json` is listed in its Authorised paths and the change is recorded in its Decisions log ("report_schema_v0.json added to Authorised paths before implementation"), so AC8 landed without the `additionalProperties: false` collision this entry predicted
- [x] defect — `scripts/check_item_scope.py::_path_matches` only supports an exact-path match or a `/**`-suffix directory wildcard; it does not support a mid/end single-star glob like `dir/*.ext`, which is exactly the pattern used for `tests/corpus/golden/*.json` in the `## Authorised paths` section of multiple item specs (108, 110, and at least 081/090/098/099/105/106/111 by grep). Running `python scripts/check_item_scope.py <spec> --base <branch>` on any item that legitimately regenerates the committed goldens (as item 108 and item 110 both do) therefore always reports every changed `tests/corpus/golden/*.json` file as "not authorised", a false positive independent of the item's actual scope. Confirmed for item 110: the only reported violations were the 9 golden files explicitly listed in the spec's Authorised paths. Either `_path_matches` needs real glob support (`fnmatch`) or every spec using this pattern needs `tests/corpus/golden/**` instead — the latter is a much larger blast-radius change across already-merged specs, so the script fix is the likely right target *(item 110, 2026-08-14)* → **item 117**: this claim was already stale by the time it was ticked — `_path_matches` had in fact grown per-segment `fnmatch` support before the script was retired, which is exactly why `tests/corpus/golden/*.json` worked in practice. The script (and this entry's premise) is now moot: `scripts/check_item_scope.py` is deleted in favour of the framework verb `aide scope`, which never had this limitation
- [x] gap — item 109's spec (AC3/AC4) says "``MetricSpec`` gains an optional reference-excursion field", but `MetricSpec` is defined in `src/segfacet/eval/per_mode.py`, which the same spec's Authorised paths list excludes and whose Assumptions section explicitly bars ("`eval/per_mode.py` is not modified by this item... if that requires touching per_mode.py, hand back and coordinate with item 112"). The two constraints are directly in tension: satisfying AC3 literally requires editing a file the spec forbids editing. Tests were written against a reconciled reading (a new, additive, `per_mode_cohort`-local companion table -- `MODE_SCALE_SPECS`/`ModeScaleSpec` -- carrying the classification and the reference-excursion field, rather than a literal edit to `per_mode.MetricSpec`), but the spec itself should be corrected so a builder doesn't have to make the same judgement call independently *(item 109, 2026-08-14)* → verified resolved 2026-08-25: item 109's spec now carries an inline "*(Corrected 2026-08-14: this AC originally said MetricSpec gains the field…)*" note on the AC itself, pointing at the additive `ModeScaleSpec`/`MODE_SCALE_SPECS` table the tests were written against and naming item 112 as `eval/per_mode.py`'s owner — so the spec no longer requires a builder to re-derive the reconciliation independently
- [x] gap — item 109's AC16 demonstrator (`tests/test_101_per_mode_cohort.py`'s `demonstrator_comparison` real-corpus fixture) turns out to depend on the very saturation bug item 109 fixes: stripping stray islands from `mode3_inject_islands` reconstructs the candidate to exactly GT, so modes 1/2/3 all land exactly on their own baseline and all saturate to `abs(normalised_delta) == 1.0` under the pre-109 formula -- a dramatic-looking "large fraction of its excursion" that was actually an artefact of the bug, not a real signal. Under the fix, mode 2's genuine (un-saturated) movement on this real fixture is ~0.0007 (single-voxel-scale), nowhere near "large". The Stage 18 demonstrator's *headline claim* (a real corpus case where an unbounded per-mode metric's magnitude dramatically beats aggregate Dice) has no real fixture backing it post-fix; only a hand-built synthetic fixture (as `test_ac16_attributed_mode_is_two` now uses) can currently demonstrate it. Worth a corpus case designed with genuinely large per-case magnitude if the Stage 18 thesis is to be demonstrated on real data again *(item 109, 2026-08-14)* → routed to `roadmap.md`'s Backlog as "Does the Stage 18 thesis have any real-data demonstrator left?" — recorded as a rider on Stage 21's perturbation corpus, since what it needs is a corpus case designed for genuinely large per-case magnitude, not a new idea. The finding that the old demonstrator's excursion was a saturation artefact rather than a signal is carried across verbatim
- [x] automation — item 111's AC4 ("no other unpinned byte-reproducible fixture remains") is discharged by a hand-maintained `_KNOWN_BYTE_EXACT_FIXTURE_FAMILIES` tuple in `tests/test_111_golden_guard.py`, populated by a one-time manual grep over `tests/` for `read_bytes()`/golden `.read_text()` comparisons. It correctly covers today's families (verified independently during validation), but nothing re-runs that grep automatically: a future item that adds a new committed byte-exact fixture and forgets both the `.gitattributes` pin and this list's entry gets a silently-green AC4 test forever, reopening the exact items-099-101 Windows-CI class of bug this item exists to close. A generic `.gitattributes`-vs-`tests/**` static scan (walk test source for `.read_bytes()`/golden `read_text()` calls against a path under a committed dir, cross-check each resolved family against `.gitattributes`) would make the survey self-maintaining instead of a point-in-time snapshot *(item 111, 2026-08-14)* → delivered by engine 1.19.0's `gitattributes_eol_pin_warnings` in `aide check` — resolves a fixture path through the AST and warns when no `eol=lf` pattern covers it; validated against this repo (zero false positives, six byte-exact comparisons correctly read as pinned). Precision over recall by design: a path reached through `tmp_path`, a function argument or an imported constant resolves to nothing and is skipped, so treat a warning as authoritative and its silence as partial — `_KNOWN_BYTE_EXACT_FIXTURE_FAMILIES` in `tests/test_111_golden_guard.py` still carries the families the lint cannot resolve
- [x] framework — `scripts/check_item_scope.py`'s default `--base main` is stale on this checkout: local `main` sits at the queue-015 commit while the current item branch already carries ~23 unmerged item commits (093-116) on top, so running the scope check with no `--base` override reports every file those prior items touched (~90 files) as "not authorised" by the *current* item's spec, regardless of what the current item actually changed. Confirmed via `git stash`: the violation list is byte-identical with and without item 113's own edits staged. The CI job itself is unaffected (it always passes `--base` with the workflow's own `origin/<base_ref>` expansion, the real PR base), so this is purely a local-invocation footgun — an item spec's own "run the scope check" validation step, followed literally with no `--base`, produces a wall of false positives that has nothing to do with the item being validated. Worth a doc note (or a smarter default, e.g. `git merge-base HEAD origin/main`) so a builder/validator doesn't misread this as their own scope violation *(item 113, 2026-08-15)* → **resolved: aide-loop 1.7.0** — `aide scope` (the promoted verb) defaults to the merge-base with `origin/<main>`, exactly the smarter default this entry proposed; note the *project* script keeps the stale `--base main` default until it is retired (see the open `_path_matches` entry above) → **item 117**: the project script is now retired — `scripts/check_item_scope.py` is deleted and the `scope-check` CI job runs `aide scope` directly, so this footgun no longer exists in this repo at all
- [x] framework — `aide check`'s unfilled-template-slot rule cannot distinguish a genuinely unfilled document slot from GitHub Actions expression syntax quoted deliberately in prose, and hard-errors (not warns) on the latter. Any living document that discusses `.github/workflows/*.yml` — an item spec explaining what a CI step runs, an insight recording a workflow's arguments — trips it, turning `aide check` red on content that is correct as written. Hit three times in queue-016 alone: twice in item 107's spec (reworded to `origin/$BASE_REF` prose) and once in item 113's insight entry, where the fix was to describe the expansion instead of quoting it. The workaround is always to stop naming the real syntax, which makes the documentation worse precisely where accuracy matters. A slot rule that ignores matches inside backtick code spans, or that recognises the Actions expression form (a `$` followed by doubled braces) as foreign syntax rather than an AIDE slot, would remove a recurring papercut whose only current remedy is to write something less true. This very entry demonstrates the point: naming the offending syntax literally made `aide check` fail, so it had to be described in words instead *(item 114, 2026-08-15)* → aide-loop issue #22, fixed in PR #32 (engine 1.5.0, installed here 2026-08-17): the pattern now exempts a `$` immediately preceding the braces and nothing else (AIDE slots are never `$`-prefixed), so a living document may name GitHub Actions expression syntax literally. Suppressing matches inside backtick code spans was rejected as the wrong fix, since the item template's own `Suggested branch` line carries a genuine slot inside one. The circumlocution in this entry's own text above stays exactly as captured (append-only)
- [x] framework — `aide progress set` unconditionally re-ticks every unticked acceptance box in any stage its own rollup derives as complete, which makes a deliberate, honest "unticked box in a still-✅ stage" state (item 114(b)'s whole point, and the maintainer-endorsed precedent that a stage may be ✅ with a recorded unmet criterion — Stage 14 closed ✅ with two Outcome targets marked not-met) impossible to keep durable from inside a consumer repo. Mechanism: `set_item_status` (`.aide/scripts/aide.py:580`) recomputes rollups for every stage in `progress.md` on every call, and at lines 611-612 runs `if derived == "complete": _tick_acceptance(lines, start, end)`; `_tick_acceptance` (line 502) flips every `- [ ]` inside that stage's line range to `- [x]` unconditionally, with no way to mark a box as deliberately-unticked/exempt. Proved with a pure-function probe against the real `progress.md` (no repo mutation): before, progress.md:744 (Stage 17's fourth acceptance box) read `- [ ]`; simulating `progress set 114 complete` re-ticked it to `- [x]`; simulating `progress set 115 in-progress` also re-ticked it; simulating `progress set 042 complete` — a wholly unrelated item in a different stage — re-ticked it too, because the function reprocesses every stage's rollup on every call regardless of which item triggered it. A real `progress set 114 done` call during this item's own build changed exactly 3 lines: line 744 plus the two Stage-26 D6/D7 deliverable bullets it was meant to touch, confirming the box re-tick is a real side effect, not a probe artifact. Consequence: because `.aide/**` is generated and must never be hand-edited in a consumer repo, item 114(b)'s untick cannot be made durable here — `tests/test_114_documentation_corrections.py::test_ac4_fourth_acceptance_box_is_unticked` will fail the moment *anyone* runs `aide progress set` for *any* item, and that failure is a deliberate tripwire surfacing this defect, not flakiness. The fix belongs in `aide-loop`: `_tick_acceptance` should not force-tick a box that was deliberately left unticked (e.g. skip boxes whose annotation already exists / is unchanged, or add an explicit exemption marker) *(item 114, 2026-08-15)* → aide-loop issue #22, fixed in PR #32 (engine 1.5.0, installed here 2026-08-17): resolved more thoroughly than this entry proposed — rather than making the derived tick exemptible, `aide progress set` no longer ticks acceptance boxes at all, on the grounds that a derived tick is not an attestation and the tick had no readers (the rollup skips checkbox lines and no `aide check` rule gates a ✅ stage on them). Ticking is now the explicit `aide progress accept <stage> (--criterion N | --all) [--evidence "…"]`, and `conventions.md` §1 states that a stage may be ✅ with an unticked box. `progress.md:744` is therefore durable, and `tests/test_114_documentation_corrections.py::test_ac4_fourth_acceptance_box_is_unticked` is now a plain assertion rather than the deliberate tripwire it was written as
- [x] framework — the same force-tick defect logged directly above (item 114, 2026-08-15) has a second live instance: item 115's Stage 26 acceptance replay found one of the stage's five acceptance criteria honestly **not** met -- a `_PRE_NNN_*`-named byte-hash fence (`tests/test_098_stray_components.py::test_ac18_reference_verse_v1_bytes_unchanged`'s `_PRE_098_REFERENCE_VERSE_V1_SHA256`) still remains, outside item 115's authorised paths to retire -- so `progress.md:1067`'s fifth Stage 26 box was left deliberately `- [ ]` with a reason annotation, the same shape as the Stage 17 tripwire box. The moment a later `aide progress set 115 done` call makes Stage 26's own rollup derive "complete" (once item 115's deliverable bullet itself flips to ✅), `_tick_acceptance` will force that box to `- [x]` too, silently converting a recorded "not met" into a false "met" -- exactly the failure mode the existing entry describes, on a second stage. No workaround was attempted here (item 115's authorised paths do not include `.aide/**`); recorded so whoever runs the next `progress set 115 done` does not read the resulting tick as a real re-verification *(item 115, 2026-08-15)* → same fix as the entry above (aide-loop issue #22, fixed in PR #32 (engine 1.5.0, installed here 2026-08-17)): `progress.md:1082`'s fifth Stage 26 criterion now stays honestly unticked, and no future `progress set 115 done` call will re-tick it. The `_PRE_098_REFERENCE_VERSE_V1_SHA256` fence the box records as outstanding is untouched by this and remains real outstanding work
- [x] defect — item 107's AC1 grep for "no `_PRE_NNN_*` fence remains" was scoped to five specific item-number prefixes named in its own Acceptance Criteria text (`_PRE_099_*`, `_PRE_100_*`, `_PRE_101_*`, `_PRE_103_*`, `_PRE_105_*` — the five items whose fences item 107 was created to retire), not a generic `_PRE_[0-9]+` regex sweep across every item number ever used in `tests/`. `tests/test_098_stray_components.py`'s `_PRE_098_REFERENCE_VERSE_V1_SHA256` was therefore never in scope for that grep at all — it names item 098, which is outside the five-item list, even though the constant's *shape* is identical to the five that were retired. This is consistent with (and gives the mechanism for) the `insights.md` entry already logged at item 116's 2026-08-12 finding ("item 107's fence retirement was incomplete... the audit needs to search by assertion shape... rather than by constant name") — confirmed by item 115's independent AC8 shape-based audit, which is the fix the 2026-08-12 entry called for *(item 115, 2026-08-15)* → explanatory, and its remedy has landed: item 115's AC8 shape-based audit is the search-by-assertion-shape the 2026-08-12 entry called for. The constant this entry explains the miss of, `_PRE_098_REFERENCE_VERSE_V1_SHA256`, is still in `tests/test_098_stray_components.py` and is routed to `roadmap.md`'s "Carried defects" section — where the disposition is to relocate it as an artifact-integrity test, not delete it. Verified 2026-08-25
- [x] gap — `tests/test_098_stray_components.py::test_ac18_reference_verse_v1_bytes_unchanged`'s `_PRE_098_REFERENCE_VERSE_V1_SHA256` is a genuine judgement call, not a clear-cut fence, on reflection during item 115's AC8 audit: `reference_verse_v1.json` is a released production artifact (80 real VerSe19 subjects, the CLI's actual default reference since item 090) that arguably *should* never change silently, so pinning its bytes is closer to a legitimate artifact-integrity invariant than to the diff-time "item N didn't touch file X" claim item 107's fences encoded. The counter-argument that still applies: it lives in `test_098_stray_components.py` (a module about stray-component detection, unrelated in subject) under a `_PRE_098_` name whose own docstring reads "item 098's state" — so a legitimate future regeneration of `reference_verse_v1.json` (e.g. a real re-fit, or a corrected VerSe19 ingest) will fire a assertion that reads, out of context, like an accidental scope violation rather than "this artifact's integrity check tripped." If this invariant is worth keeping, it belongs as a properly-named artifact-integrity test living near `reference/artifact.py` or the reference-bundling code, not inside an unrelated item's regression module. Not fixed here: `tests/test_098_stray_components.py` is outside item 115's authorised paths *(item 115, 2026-08-15)* → routed to `roadmap.md`'s "Carried defects" section 2026-08-25, with this entry's judgement adopted: the invariant is worth keeping and the module it lives in is the defect, so the candidate item relocates it to a properly-named artifact-integrity test beside `reference/artifact.py` rather than deleting it. Confirmed still present — `_PRE_098_REFERENCE_VERSE_V1_SHA256` at `tests/test_098_stray_components.py:1035`
  - **2026-08-25** → **2026-08-25** → two additions from the engine 1.20.0 upgrade. First, this entry's judgement is the framework's documented disposition, not just this repo's: `.aide/conventions.md` §1 separates a diff-time scope claim (belongs under **Asserts against**, retires with its item) from an artifact-integrity invariant, which is "legitimate and durable — but then it belongs in a test named for the artifact, living beside it, not inside an unrelated item's regression module under a `_PRE_NNN_` name". That rule predates the upgrade; it was simply never cited here. Second, engine 1.19.0's new `eol=lf` lint cannot see this test — it resolves fixture paths through the AST from `Path(__file__)` and string literals, and this one calls `bundled_production_reference_path()`, so it is skipped in silence. The file is pinned only by the unrelated glob `src/segfacet/reference/reference_verse_*.json`, so a relocated test written through the same helper stays invisible to the lint and must keep its pin by hand
- [x] knowledge — `tests/test_102_stage18_validation.py:826` carries a stale section header, `# AC24: the scope fence -- no production code changed by this item`, directly above `_SRC_TREE_HASH_AT_COLLECTION` (lines 826-853), which by item 115's own AC8 discriminator is an intra-run determinism assertion (hashed at collection time, re-hashed and compared within the same run), not a fence at all — item 107 retired the actual byte-hash-vs-hardcoded-literal fences, and this header's wording never got updated to match. This is exactly the failure mode item 115's own AC8 entry above names for the missed `_PRE_098` case: text that *reads* like a fence, sitting where the next name-or-text-shaped audit (human or grep) will find it first, risks either a false "still a fence" flag or, worse, a future maintainer retiring a legitimate assertion believing it one. Not fixed here: `tests/test_102_stage18_validation.py` is outside item 115's authorised paths, so renaming the header (e.g. to name the no-mutation/determinism check it actually is) is left for whoever next legitimately opens that file *(item 115, 2026-08-15)* → routed to `roadmap.md`'s "Carried defects" section 2026-08-25. Confirmed still present — the `# AC24: the scope fence` header sits directly above `_SRC_TREE_HASH_AT_COLLECTION` in `tests/test_102_stage18_validation.py`, and the assertion beneath it is the intra-run determinism kind that must stay, so renaming the header is the whole fix
- [x] defect — a test that shells out and parses the child's stdout **passes vacuously when the capture comes back empty**, and queue-016's PR CI produced exactly that capture failure on Windows: `tests/test_114_documentation_corrections.py`'s `_run_aide_check` used `subprocess.run(..., capture_output=True, text=True)`, which is documented to populate both streams, yet `proc.stdout` was `None` on the `windows-latest` runner (`TypeError: unsupported operand type(s) for +: 'NoneType' and 'str'`) while every Linux gate — local suite, item 115's two fresh-clone runs, both validator rounds — stayed green. Not reproducible on Linux, so the mechanism is still unknown; what matters more is the **shape**: had the capture returned `""` instead of `None`, the caller's `for line in output.splitlines()` would have iterated over nothing and the test would have reported PASS while checking absolutely nothing — the same silently-green class as item 099's hardcoded-absolute-path bug, which also survived every local gate. Fixed by deleting the subprocess boundary rather than hardening it: `aide check`'s logic is `run_checks(repo_root, config)` (`.aide/scripts/aide.py:1004`), a stdlib-only in-repo function returning `(errors, warnings)` as structured data, so the test now calls it in-process — no stdout, no encoding, no platform surface, no re-parsing of text that was structured a moment earlier — plus a non-empty assertion so a plumbing failure fails loudly instead of vacuously. General lesson: prefer calling the function over shelling out to the command that calls it, and when a test must parse output, assert the output is recognisable before looping over it *(2026-08-15)* → fixed at capture (the subprocess boundary was deleted, not hardened), and the general lesson is now enforced rather than remembered: `conventions.md` §6 bars a test shelling out to `aide.py` when the logic is importable, and engine 1.14.0 gave it an AST-matched lint in `aide check` — matched through the AST precisely so the docstring explaining why an author removed a subprocess is not itself flagged. `aide check` reports no such finding here as of 2026-08-25
- [x] defect — the CI scope-check job resolved an item spec from the PR branch name with `grep -oE '[0-9]{3}' | head -n1`, an unanchored match that cannot tell a **queue** number from an **item** number: `aide/queue-016` read as item 016 and resolved `docs/aide/items/016-features-json-serialization.md`, a queue-002-era spec that predates item 107's `## Authorised paths` convention, so the checker hard-errored (exit 2, "missing or empty '## Authorised paths' section") on an item the branch had nothing to do with. Surfaced the first time a queue branch was ever PR'd — every prior run was an `aide/NNN-…` item branch, where the unanchored match happened to be right. Fixed by anchoring to the claim protocol's actual branch shape (`.aide/conventions.md:245`, `aide/NNN-short-name`) so queue and specs branches resolve to nothing and skip, which is correct: per-item scope is already enforced on each item branch as it merges into the queue branch, and a queue branch legitimately aggregates many items' authorised paths. Worth noting for the framework that queue numbers and item numbers share one namespace with no syntactic marker distinguishing them — anything parsing a branch name, spec filename or reference must anchor on position rather than pattern-match a bare 3-digit run *(2026-08-15)* → **resolved: aide-loop 1.13.0** — the framework-side lesson landed twice: `aide scope` reads the item from the claim branch via the anchored branch→item helper (1.7.0), and 1.13.0 fixed the last unanchored call site, `_pick_item`, where `aide/queue-016` read as item 016 and made that item permanently unclaimable
- [x] framework — **third instance of the `str(Path)` OS-separator class**, this time in `aide check`'s own output: `.aide/scripts/aide.py:704` and `:792` both build a warning location as `f"{path.relative_to(ddir)}:{lineno}"`, and f-stringing a `Path` calls `str()`, which renders the OS-native separator — so the identical document reports as `queue/queue-002.md:80` on Linux and `queue\queue-002.md:80` on Windows. Only locations with a subdirectory component diverge, which is why `queue/queue-002.md` was the single entry that broke the Windows CI leg while the six `docs/aide/`-root locations (`progress.md`, `insights.md`) passed — a partial failure that reads as a content problem rather than a platform one. The fix is `.as_posix()`, exactly as for the two earlier instances recorded above (the item-099/100/101 `_combined_hash` package-fence bug and its single-file predecessor), but `.aide/**` is generated and must not be hand-edited in a consumer repo, so the workaround here normalises separators test-side (`tests/test_114_documentation_corrections.py::_posix_location`, with a Linux-runnable regression guard). Any consumer that parses `aide check` output — rather than calling `run_checks` and reading the structured `(errors, warnings)` — inherits this. Worth a sweep of `aide.py` for `Path`-in-f-string/`str(Path)` anywhere the result is compared, hashed, or matched, since this class has now cost four separate CI-only failures and every one of them was invisible to a Linux checkout *(2026-08-15)* → aide-loop issue #22, fixed in PR #32 (engine 1.5.0, installed here 2026-08-17): both warning locations now use `.as_posix()`, so the identical document reports identically on Linux and Windows. The test-side `_posix_location()` normaliser this entry describes as the workaround has been retired from `tests/test_114_documentation_corrections.py`; the broader sweep for `str(Path)`/`Path`-in-f-string comparisons the entry recommends was not part of the upstream fix and remains open
- [x] framework — `aide check`'s `unrecognised branch` warning names a problem but not its remedy, and `aide gc`'s bare-invocation "nothing to clean" is silent about the remedy that exists. On this checkout `aide/feedback-loop-triage` (carries the prefix, matches neither the item nor the queue shape) draws the warning permanently, and `aide gc` reports "nothing to clean" while it and two merged queue branches sit there — yet `aide gc --merged` offers all three as "merged into main", which `cmd_gc` (`.aide/scripts/aide.py:1734`, target selection at :1764-1776) documents as exactly the ground queue/specs branches stay eligible on once they resolve to no item. So this is a signposting gap, not a missing capability: suggest the warning end with the action ("delete it, or rename to `<prefix>NNN-short-name` -- `aide gc --merged` will offer it once merged"), and that `gc`'s empty default result mention `--merged` when merged prefixed branches exist. Recorded after first mis-diagnosing this as an absent cleanup path on the strength of bare `aide gc` alone, which is itself the evidence for the signposting complaint *(2026-08-17)* → **resolved: aide-loop 1.5.1** — all three signposts now name their remedy
- [x] framework — the human-gate reach check (engine 1.13.0, `gate_warnings` → `stage_item_numbers`, `.aide/scripts/aide.py:1079`) cannot tell a **typo** from a **not-yet-queued stage**, and reports the second as the first. Raising the two external-data gates on this repo's Stage 16 — a 📋 stage whose deliverables carry no `*(Item NNN)*` refs because no item has been created for it yet — yields `blocks stage 16 — which has no deliverable referencing any item, so this gate holds NOTHING; check the stage number`. The stage number is correct, and the situation is precisely the one 1.13.0's own changelog names as the primary use (*"a `roadmap.md` stage declares one known at planning time (usually `Blocks: stage N`)"*): a gate raised **before** the work is queued, which is the only time raising it is cheap. As written the framework's documented happy path emits a warning telling the author their stage number is wrong. Cheap fix: split the two cases — if no stage section with that number exists, keep the current "check the stage number" wording; if the section exists but references no items, say so neutrally ("stage N has no items queued yet — this gate will block them as they are created") since the gate *is* correctly armed and `aide claim` will honour it the moment `stage_item_numbers` becomes non-empty. Note the warning is otherwise well-designed — it is the only thing standing between a mistyped stage number and a gate that silently guards nothing *(2026-08-18)* → **aide-loop issue #48** — filed 2026-08-20; re-verified live against engine 1.14.0 first (both Stage 16 gates still warn `check the stage number` verbatim, and the stage section exists at `progress.md:697`). The issue carries the split-the-two-cases fix and quotes 1.13.0's own changelog, which names the not-yet-queued stage as the primary use
- [x] gap — this repo's pytest config sets `testpaths = ["tests"]`, so the framework's own suite under `.aide/scripts/tests/` (11 modules, including `test_aide_scope.py`, which is the *only* live coverage of the `scope` verb once item 117 retires the project script) is never collected by `python -m pytest` here. An engine update therefore lands in this checkout with its own tests unrun locally and unrun in CI, and the consumer only finds out when a verb misbehaves in the loop. Options: add `.aide/scripts/tests` to `testpaths` (it is stdlib-only + pytest, so it costs nothing to run), or add a CI step that runs it explicitly after an engine bump. Noted while specifying item 117, which deliberately adds only a narrow consumer smoke test rather than duplicating that suite *(item 117, 2026-08-19)* → **fixed in this repo (item 117, AC19, 2026-08-20)** — took the first option: `testpaths = ["tests", ".aide/scripts/tests"]`. Collection verified at 5437 (5075 project + 362 framework), no name collisions and no conftest needed; ~6s on every leg. AC19 pins both the config entry and that the suite it points at really carries scope coverage, since a path in `testpaths` proves nothing if the file behind it is empty. The framework half is a separate entry below
- [x] framework — the installer ships `.aide/scripts/tests/` (11 modules, 362 tests) into every consumer and says **nothing anywhere** about whether a consumer should run them: `grep` for `scripts/tests` across `.aide/README.md`, `.aide/conventions.md` and `.aide/templates/` returns zero hits, and `aide.toml`'s `[project] test_command` / `tests_dir` describe only the *project's* suite. So the default outcome in any consumer whose pytest config sets `testpaths` (a near-universal convention) is that an engine update lands with its own tests never run — locally or in CI — and the consumer discovers a broken verb mid-loop instead. This repo hit exactly that: engine 1.5.0 → 1.14.0 was installed and its 362 tests ran only because they were invoked by hand; a bare `python -m pytest` collected none of them. Fixed *here* by item 117's AC19, but every consumer will re-derive it independently, which is the signature of a framework-side gap rather than a project one. The fix is signposting rather than capability (the 1.5.1 pattern): `install.py` could name the directory in its completion output, and `.aide/README.md`'s file table could say whether the suite is meant to be collected — stating the answer either way, including "deliberately not; run it by hand after an update" if that is the intent. Related question worth answering in the same breath: what a consumer should do when a shipped test goes red, given `.aide/**` must never be hand-edited (presumably "update or roll back the engine", but nothing says so) *(item 117, 2026-08-20)* → **aide-loop issue #49** — filed 2026-08-20; re-verified against engine 1.14.0 first (`grep -rn 'scripts/tests'` over `core/README.md`, `core/conventions.md`, `install.py` and `core/templates/` is still empty, and the README file table still has no row for it). Filed as `documentation`: a README row plus a line in the installer's completion output, and an answer to what a consumer does when a shipped test goes red
- [x] defect — **the `scope-check` CI job cannot fire under this repo's own workflow, so "verify it on the next suitable PR" may never arrive.** The job is guarded by `if: github.event_name == 'pull_request'` and resolves its item number from `github.head_ref` via `sed -n 's|^aide/\([0-9]\{3\}\)-.*|\1|p'`, so it only does anything when a PR is opened *from an item claim branch*. But `aide.toml` sets `[git] mode = "auto-merge"`: `aide merge NNN` merges an item branch into its base locally and deletes it, so an `aide/NNN-` branch never becomes a PR. And under the stage/queue-PR model now emerging (one reviewed PR per queue, sub-branches per item, which engine 1.8.0's `--base` was built for), the only PR that *is* opened has `head_ref = aide/queue-NNN`, which the anchored sed deliberately resolves to nothing — correctly, per the ticked entry above: a queue branch legitimately aggregates many items' authorised paths. Net effect: every PR is either a `chore/`-style framework PR or a queue PR, and both skip. Confirmed twice on PR #52 (engine 1.14.0): the job reported SUCCESS both times having logged `could not extract a 3-digit item number from branch 'chore/framework-1.14.0'; skipping scope check`. It was reachable historically — the ticked entry above notes "every prior run was an `aide/NNN-…` item branch" — so this is a gate that decayed as the branching model changed, not one that never worked. **Per-item scope is still enforced**, but only by `validator.md` step 3 running `aide scope` in-loop on the same machine, which is exactly the single-platform blind spot conventions §7 exists to name. Options, in rough order of cost: (a) retire the job and state plainly that the validator is the gate; (b) give the queue PR a job that enumerates the items merged into the queue branch and runs `aide scope NNN --base <queue base>` for each — the real answer for the queue-PR model, and what `--base` enables; (c) add `workflow_dispatch` with an item-number input as a manual stopgap. Do not simply wait for an item PR to appear *(item 117, 2026-08-20)* → routed to `roadmap.md`'s "Carried defects" section 2026-08-25, carrying all three options. Still live and re-confirmed on this session's own framework PR branch, whose head is `chore/framework-1.20.0` and matches the job's `sed` no better than the queue branches do. The framework half is closed separately — engine 1.18.1 answered aide-loop issue #50 by stating in `conventions.md` §4 what CI gate each `git.mode` makes possible — so what remains is this repo's own workflow file
  - **2026-08-25** → **2026-08-25** → re-assessed against engine 1.20.0, which shifts the choice between the three options toward (a), switching `[git] mode` to `pr`. That route used to carry a branch-deleting footgun unrelated to the CI question — `✅` meant "pushed, awaiting review" under `pr` while `aide gc`'s `✅` ground deleted local *and* remote without asking git whether the work had landed, so queue exhaustion offered to delete an open PR's head. 1.20.0 closed both halves: `✅` means merged in every mode with `🔍` as the state between, and `gc` now asks `git merge-tree` and skips a branch carrying unlanded content. Switching modes makes the existing job work as designed with no workflow change at all
- [x] knowledge — partial routing of the `scope-check` defect logged directly above (still open): its **framework half** is now handed over as **aide-loop issue #50** (2026-08-20) — `aide scope` has no CI-reachable surface under either `[git] mode = "auto-merge"` (item branches are merged and deleted locally, so no `aide/NNN-` PR ever exists) or the queue-PR model (the only PR head is `aide/queue-NNN`, which `cmd_scope` deliberately short-circuits with "Nothing to check"), so `conventions.md`'s claim that "per-item scope is checked on each claim branch as it merges" holds only for the validator's in-loop run on one machine. The issue proposes (a) saying so plainly in `conventions.md` or (b) teaching a queue branch to enumerate the items merged into it and check each against the queue base — this entry's option (b), which 1.8.0's `--base` already enables. **The project half is NOT routed**: `.github/workflows/ci.yml`'s `scope-check` job is consumer-owned (the framework ships no CI template) and still reports SUCCESS while doing nothing on every PR this repo actually opens, so the entry above stays unticked pending a fix item in the next queue *(2026-08-20)* → framework half landed in engine 1.18.1 — aide-loop issue #50's option (a): `.aide/conventions.md` §4 now carries a per-`git.mode` table of what CI gate is possible, and names the governing distinction as PR context rather than branch visibility. The project half is still open as entry 75 above: `.github/workflows/ci.yml`'s `scope-check` job remains consumer-owned and still reports SUCCESS while matching nothing under `auto-merge`
- [x] knowledge — the `scope-check` entry above lists three remedies and misses the cheapest: **`[git] mode = "pr"`**. The job is not broken, it is *mode-dependent*, and the entry above overstated the finding by treating this repo's `auto-merge` setting as the workflow. Per `.aide/conventions.md` §4 the three modes differ exactly here — `auto-merge` direct-merges a validated item and deletes the branch, `local` merges offline with no pushes, but **`pr` pushes the branch and stops for a human to open the PR** — and that PR's `head_ref` *is* an `aide/NNN-` claim branch, which is precisely what the job matches. It also works under the stacked queue-branch shape, since the job passes `origin/${{ github.base_ref }}` and the base there is the pushed queue branch. So the decision is not only "how do we fix CI" but "do we want a PR per item at all" — `auto-merge` buys unattended throughput and costs the independent scope signal; `pr` buys the signal back and costs a human open per item, which the queue-PR shape was adopted to avoid. Worth deciding deliberately rather than inheriting: the two axes (branch shape, merge mode) are independent, and only the mode governs whether the CI gate can ever run *(item 117, 2026-08-20)* → **aide-loop issue #50, rewritten** (2026-08-20, before any response on it): this correction is now the issue's lead. Verified against `core/conventions.md` §4 — `pr` mode does push the claim branch and stop for a human PR, so `head_ref` is `aide/NNN-`, and this repo's job already passes `--base "origin/${{ github.base_ref }}"`, which resolves to the queue branch under the stacked shape. Issue retitled from "aide scope has no CI-reachable surface…" to "`git.mode` silently decides whether `aide scope` can ever run in CI, and conventions §4 does not say so", relabelled `enhancement` + `documentation`, and its option (a) promoted from "say the gate does not exist" to "document the trade where the mode is chosen". A ~Corrected~ banner and a "What was overstated" section record the original claim, so the framework maintainer sees the retraction rather than a silently better issue
- [x] framework — **nothing in the framework says a durable artifact must be readable without the conversation that produced it.** `grep` for `self-contained`, `read cold`, `chat-local` or `outlives` across `.aide/conventions.md`, `.aide/templates/item.md` and `CLAUDE.md` returns zero hits, yet every artifact the loop produces is exactly this kind of thing: item specs, `insights.md` entries, commit messages, and the GitHub issues `/aide-feedback-loop` files into `[framework] repo`. Three rules, each with a recorded instance from this repo's own history: (1) **no chat-local identifiers** — labels coined for conversational convenience ("A1–A4", "Wave 1", "the D-series") are scaffolding, not names; a reader opening the issue sees "A1" and cannot tell what the A-series was or what happened to B; (2) **cross-reference by resolvable identity** — `#25`, a file path, a dated insights entry, never "the conventions issue" or "the companion PR"; (3) **record the decision and why it holds, not the route to it** — "my earlier lean was wrong", "agreed direction", "settled while drafting" narrate process and age badly. This is provider-agnostic and belongs in `core/conventions.md` §1 beside the format contract, not in each consumer's `CLAUDE.md`: it governs the *framework's own* artifact shapes, so every consumer otherwise re-derives it or (more likely) never states it. Adopted locally in this repo's `CLAUDE.md` ("Durable artifacts must read cold") pending that; if the framework takes it, the local copy should be deleted rather than left to drift. Originally captured as a personal note while filing the aide-loop issue backlog, which is exactly the wrong home — a convention every contributor must follow cannot live somewhere only one machine can see *(2026-08-20)* → **aide-loop issue #59**, filed with a second half the entry did not have: the framework has **no channel into a consumer's default context at all** — `core/templates/` ships six document templates and no instruction-file template, and `install.py` never references `CLAUDE.md` (grep returns nothing). So every framework rule lives in `.aide/conventions.md`, read only when something points at it, which holds for agent specs in the unattended loop but not for an interactive session creating issue bodies, commit messages and insights entries with no agent spec in play. Provider independence resolved by copying `ADAPTER-SPEC.md` §5's existing split for command hygiene: the rule text is runtime-general and lives in `core/conventions.md`, while *which file the runtime reads by default* becomes a new optional §7 an adapter declares (`CLAUDE.md` for the Claude adapter), omitted by runtimes with no such concept. Scoped small deliberately — one delimited managed block the installer refreshes and `--check` reports drift on, with per-provider content variants and multi-file support explicitly deferred
- [x] framework — **`insights.md`'s append-only rule forbids what its own checker permits and what practice already does.** `.aide/conventions.md` §1 allows "exactly two exceptions" — ticking the box and appending one `→ where it landed` pointer, both at triage — so there is nowhere to record that an entry's premise later decayed. But `insight_warnings()` (`.aide/scripts/aide.py:936`) applies `_INSIGHT_RE`, which anchors the line start and requires a `*(date)*` somewhere, then stops: a second trailing pointer and an indented continuation bullet each produce **zero warnings** (verified by calling the shipped function directly — indented lines are skipped by `if not line.startswith("- ")`). This file already carries **17 entries with two or more `→` pointers** out of 77. Rot has a worked example here: the 2026-08-12 entry naming three surviving byte-hash fences is now part-stale — `_PRE_ITEM_REFERENCE_DEFAULT_SHA256` is gone from `tests/test_093_tptbox_label_convention.py`, while `tests/test_098_stray_components.py` (8) and `tests/test_107_item_scope_check.py` (6) still carry `_PRE_` constants — and nothing records which part. The fix separates the two things the rule conflates: the captured **claim** stays immutable (provenance — several entries here are wrong and instructively so), while its **status** becomes an appendable dated trail. No checker change needed *(2026-08-20)* → **aide-loop issue #51**, which also carries the second half: `framework`-typed triage is coupled to the queue boundary for no reason (it routes to an issue tracker, not a queue item), which is why 13 of the 15 entries open at filing time all date from 2026-08-12..15
- [x] framework — **`insights.md` is the only continuously-written living document with no CLI verb**, which is why triage is expensive enough to keep deferring. `progress.md` has `aide progress` and `aide gate`, `queue/` has `aide queue tidy` and `aide claim --queue`, `items/` has `aide claim`, `aide scope` and `aide check --queue` — the inbox has `aide check`'s shape lint and nothing else, so every triage pass means an agent reading and hand-parsing the whole file. Measured here on 2026-08-20: **77 entries, 110,867 chars ≈ 29k tokens, median entry 1,437 chars** against a stated "one line" rule, longest 3,219; **74 dated entries over 30 days ≈ 2.5/day**, extrapolating to ~900 entries and ~1.1 MB in a year. The live working set was **15 open** — the reader pays for all 77 because open and closed are interleaved. Proposed verbs: `insights list [--open] [--type T]`, `insights tick NNN --pointer …`, `insights archive --before YYYY-MM-DD` (closed entries to `insights/archive-YYYY-QN.md`). Capture must stay exactly as it is — one line, one file, atomic, no number to allocate, no network — which is what rules out a folder-per-insight layout (number allocation at capture time) and a GitHub-issue-per-insight store (breaks `git.mode = "local"`) *(2026-08-20)* → **aide-loop issue #52**, which records two scoping snags found while assessing: `_ALWAYS_AUTHORISED` is deliberately wildcard-free so an archive path needs an explicit decision, and archived entries would fall out of the shape check (`insight_warnings` reads only `insights.md`) while staying in the slot check (`template_residue_errors` uses `rglob`). It also notes `aide.py`'s own `Subcommands::` docstring is stale — `gate` shipped in engine 1.13.0 and was never added, so it lists 10 of 11 verbs
- [x] framework — the two numbered collections under `docs/aide/` disagree on **three axes at once**: `items/NNN-slug.md` is plural-directory, no prefix, slugged; `queue/queue-NNN.md` is singular-directory, redundantly prefixed (`queue-` inside `queue/`), unslugged. Plural is the house majority (`items/`, `permissions/`, `status/`), and the `queue-` prefix duplicates the directory it sits in. Both subdirectory names are hardcoded and non-configurable — `aide.toml`'s `docs_dir` comment says so outright ("scripts derive queue/ items/ from it"). Recorded as **known and deliberately not scheduled**: harmonising breaks consumer repos mid-flight, and the cost is concrete — **352 `queue/queue-NNN` references across 110 files** in this repo's `docs/aide/` alone, 13 hardcoded call sites in `aide.py`, ~51 naming-shape references across 16 framework files, and every other consumer carries its own count. It also collides with the rule that completed queue files and merged item specs are provenance records and are not rewritten *(2026-08-20)* → **aide-loop issue #53**, labelled `deferred` (a label created for it: "known and accepted; deliberately not scheduled", distinct from `wontfix`). If ever done it should be one installer-driven migration, after the naming-helper centralisation below
- [x] framework — **item and queue numbers share one namespace, and the engine defends that only on the branch side.** `_branch_item_number`'s docstring (`.aide/scripts/aide.py:2510`) states the hazard and the damage: an unanchored digit search reads `aide/queue-016` as item 016, and `gc` then deletes an in-flight queue branch "and the unreviewed queue file and item specs living only on it". Engine 1.13.0 fixed that for branches, centralised in two anchored helpers. **Filenames got no equivalent** — the same convention is re-typed raw at 13 sites: `idir.glob(f"{n:03d}-*.md")` five times (`:754`, `:1743`, `:1819`, `:2224`, `:2867`), `qdir / f"queue-{n:03d}.md"` three times (`:1730`, `:2089`, `:2298`), `qdir.glob("queue-*.md")` twice, `int(p.stem.split("-")[1])` once (`:2095`), plus three `f"queue-{n:03d}"` in status/output text. Proposed: an `item_spec_paths` / `queue_path` / `queue_number` / `iter_queue_paths` block beside the existing branch helpers, with `queue_path` resolving by glob rather than constructing. Worth stating plainly that **this fixes no live bug** — every filename site is correct today; the value is regression containment for the class 1.13.0 fixed, and making a future convention change one edit instead of a 13-site sweep *(2026-08-20)* → **aide-loop issue #54**. Supersedes an earlier suggestion to mark the namespace with a distinct queue prefix (`Q016`), rejected because `queue-016.md` is already the marker — the defect is that the marker is re-derived at every site rather than living in one function
- [x] framework — open question whether `queue/` files should carry a slug the way `items/` files do (`queue-016-stage-27.md` naming the roadmap unit a batch covers), since a directory listing would then say what each queue was for. Costs measured: **3 sites break outright** (the `qdir / f"queue-{n:03d}.md"` direct constructions), **1 survives only by luck** (`int(p.stem.split("-")[1])` — index 1 is still the number on a slugged stem), and **~51 naming-shape references across 16 framework files** would need updating so the agents write the right filename. Two objections weigh against the affordance: a slug puts a queue number and a stage number adjacent in one filename, which is the exact adjacency that produced the `aide/queue-016`-as-item-016 misread engine 1.13.0 had to fix; and a queue is defined as "a single stage, **or a small phase**", so a stage-named slug is honest only when the mapping happens to be 1:1 and otherwise either lies or grows long and vague *(2026-08-20)* → **aide-loop issue #55**, filed as a question rather than a work item, and cheap to act on only after the naming-helper centralisation (issue #54) collapses it to a one-line change
- [x] framework — `aide check`'s insight-shape regex accepts only `*(item NNN, YYYY-MM-DD)*` or a bare `*(YYYY-MM-DD)*` as an entry's provenance (`_INSIGHT_RE`, `.aide/scripts/aide.py`), so a claim that did not originate in one numbered item has no accepted form and warns forever. Three entries in this inbox are in that state and have been since the checker shipped: one captured during queue planning, provenance `*(queue-014, 2026-07-26)*`, and two spanning three items at once, `*(items 099-101, 2026-07-27)*` — plural `items` and a range, neither matched. **They are unfixable by design**: `conventions.md` §1 makes the captured line immutable, and rewriting `items 099-101` as a single `item 099` would destroy the provenance the marker exists to record. So the three warnings are permanent noise in `aide check`, and permanent noise is what trains a reader to skim the one run where a warning was real. Queue-level and multi-item work are both ordinary — `/aide-create-queue` and `/aide-spec-queue` are queue-scoped activities that legitimately produce insights, and a cross-cutting finding spanning several items is exactly the kind worth capturing. Either the pattern should admit them (`items NNN-NNN`, `queue-NNN`) or the convention should state a single canonical form and the templates should show it, but today the checker and the practice disagree with no way to reconcile them *(2026-08-25)* → handed over as aide-loop issue #76 (2026-08-25). The issue carries a second consequence found after this line was captured: `archive` selects on `e.date is not None`, and an entry whose provenance does not parse has no date — so these three are excluded from every `--before` cut in silence, however old and however closed. Confirmed by running `insights archive --before 2026-08-01 --yes` here: 42 of 84 entries moved and all three stayed behind, leaving `aide check` at 10 warnings where 7 was expected. They are therefore neither working set nor archivable, which is a sharper problem than the warning itself
- [x] defect — `tests/test_114_documentation_corrections.py`'s AC8 baseline pins `aide check` warnings by **location** (`progress.md:365`, `insights.md:31`, …), and a location is the one part of a warning the loop's own verbs keep changing. It has now been re-pinned three times, twice on 2026-08-25 alone: the `## Human gates` table (engine 1.13.0) pushed `progress.md`'s three down by 24, the `| 🔍 | In Review |` legend row (engine 1.20.0) by one more, and `aide insights archive --before 2026-08-01` moved `insights.md`'s three from 51/58/60 to 31/32/33. **Every one was the same warning about the same document line**, and every one presented as a red test that reads like a regression until you diff the numbers. The failure mode is worse than the churn: a re-pin is a mechanical edit under time pressure, and the honest way to make it is to check each moved warning is the one you think it is — which nothing enforces, so a genuinely new warning arriving in the same run as a shift would be re-pinned straight into the baseline. The fix is to pin the warning's **text** and let its location float; the text is what AC8 actually cares about ("no new warning beyond the baseline") and it is stable across all three shifts, while still failing on a real new finding. `aide check` reports as `path:lineno: text`, so the split is a one-line change to `_LOCATION_WARNING_RE`'s use, not a redesign *(2026-08-25)* → routed to `roadmap.md`'s "Carried defects" section 2026-08-25, as a fourth entry: pin the warning text rather than its location. The immediate shift was repaired in place (51/58/60 → 31/32/33) so the suite is green, and the module's audit comment now records all three re-pins and why the design, not the numbers, is the defect
  - **2026-08-25** → **2026-08-25** → fixed, not carried: the roadmap entry this pointed at is removed. AC8 now keys its baseline on `(path, text)` with a count, dropping the line number — proven both ways rather than argued: a +2 line shift inserted into `progress.md` moved all three warnings (365→367 etc.) and AC8 passed, and a fourth malformed `insights.md` entry — byte-identical in text to three already tolerated — failed it, naming the excess. Three new tests pin the properties: the key discards only the line number, the count catches one more of an identical warning, and the three exclusion categories still apply
- [x] framework — `aide check`'s insight-entry lint accepts only a single `*(item NNN, YYYY-MM-DD)*` reference, so three entries in `docs/aide/insights.md` warn permanently and cannot be cleared from this side. Lines 31–33 are flagged solely for their reference form: `*(queue-014, 2026-07-26)*` names a queue rather than an item, and `*(items 099-101, 2026-07-27)*` (twice) names an item *range*. Both are more accurate than any single item number would be — the first claim was learned while planning a queue, the second spans three items that produced it jointly — and a captured claim is immutable under `conventions.md` §1, so rewording them to satisfy the pattern is forbidden and archiving them would only relocate the warning. Net effect is three permanent warnings in every `aide check` run, which is precisely the "a real stale claim is that much easier to miss among them" failure mode the queue-branch entry above (resolved in engine 1.5.0) described for a different lint. Fix belongs in the framework's entry pattern: accept `queue-NNN` and `items NNN-NNN` alongside `item NNN`, all three still optional *(2026-08-26)* → resolved: aide-loop PR #77, engine 1.21.0 (installed here 2026-08-26) — an insight's provenance is now free-form and only the ISO date is load-bearing, so `queue-NNN` and `items NNN-NNN` parse. Both symptoms are closed: the three warnings are gone from `aide check`, and `archive --before` now dates all three entries (40 of 43 closed entries moved before, 43 after). The engine also reports closed-but-undatable entries rather than skipping them silently
  - **2026-08-26** — the same root cause has a second, worse symptom: these three entries can never be archived either, so they are stuck in the live inbox permanently. `archive_insight_text` (`.aide/scripts/aide.py:1256`) selects on `e.ticked and e.date is not None and e.date < before`, and the unparseable trailer that trips the lint also yields `date = None` — so the archiver skips exactly the entries whose warnings archiving would otherwise clear. Measured today: `aide insights archive --before 2026-08-26` moves 40 of 43 closed entries and leaves precisely these three behind. That rules out archiving as a workaround and makes the parser fix the only route. Note `insight_warnings` (`:1052`) already exempts archived entries by design, reasoning that a warning on a frozen claim names a defect no one may fix — the same reasoning applies to a live claim that is frozen by the immutability rule
- [x] knowledge — a test must not assert that a captured claim lives in `docs/aide/insights.md`, because `aide insights archive` is designed to move it to `insights/archive-YYYY-QN.md`. `tests/test_117_scope_verb_swap.py`'s two AC4 tests read only the live inbox and assert two claims are present, ticked and unrewritten, so archiving the inbox to zero — routine housekeeping, not a regression — turned both red. Widened here to search the inbox and every `insights/archive-*.md`, which asserts the same contract (the claim survives verbatim, ticked, naming its item) against the pair of files that can legitimately hold it. This is the same defect class `tests/test_114_documentation_corrections.py`'s AC8 notes already describe — pinning what the loop's own verbs are built to move, there a warning's line number, here an entry's file — and it is worth stating once as a rule for any future test that reads the inbox *(2026-08-26)* → folded into CLAUDE.md Gotchas (tests reading the insight inbox must also search insights/archive-*.md)
- [x] defect — a human gate's reach is set by its **Blocks** cell alone; prose in its *Decision / evidence* cell that narrows that reach is read by nothing and silently disagrees with what the loop enforces. `progress.md`'s gate 3 (Stage 28, spinal curve model) was authored `Blocks: stage 28` while its own evidence cell said it "blocks Stage 28's D2 (implementing the formulation) but not D1 (recording the decision and its measurements, which is what the gate reads)". `aide claim --queue 017` therefore held all eight items 118-125 — including item 118, the deliberation that exists to produce the evidence the gate is decided on, and items 122/124, which queue-017.md marks independent of it. The gate blocked the work that feeds it. `conventions.md` §Human gates already documents the fix in a two-row table: item-number reach (`119`, `119, 120`, `119-121`) is for "the decision affects one thread; the queue keeps producing other work", `stage N` for "the decision could invalidate a stage's work". This was the first row and was written as the second. Not a CLI limitation — `_blocked_item_numbers` accepts bare and §1 item references — so the correction is to the row, and it needs a person, since an agent narrowing a live gate's reach is partial approval by another name *(queue-017, 2026-08-27)* → moot since the gate's 2026-08-27 approval dissolved the hold; the authoring rule it names is conventions.md §Human gates' reach table; the check-time invisibility half handed over as aide-loop#105
- [x] framework — `aide check` warns that a human gate is awaiting but never says **how much it holds**, while `aide claim` does ("holding 118, 119, ... 125"). So a mis-scoped gate is invisible at authoring time and only surfaces when a runner stalls on it — `aide check` was run twice against gate 3 above and printed only "is awaiting a decision — blocks stage 28". The breadth is already computed at check time: `gate_blocked_items()` resolves it, and `gate_warnings()` uses the result solely to distinguish a stage that yet names no items ("holds nothing today") from one that does. Reporting the resolved count or list in that warning would have made the contradiction visible where it was introduced. Adjacent and lintable: a gate row whose evidence cell names a deliverable- or item-level carve-out while its Blocks cell says `stage N` is a detectable disagreement between what a gate says and what it does *(queue-017, 2026-08-27)* → aide-loop#105
- [x] automation — `.claude/settings.json` pre-approves writes to `src/segfacet/**`, `tests/**` and the `docs/aide/` living documents only, so any item whose deliverable is a **project tool** (`scripts/*.py`, the shape of `scripts/refresh_reference.py`) or a **durable technical document** (`docs/*.md`, the shape of `docs/reference-build.md`) stalls an unattended builder on a permission prompt for each file — item 118 writes one of each. Both directories already hold committed, reviewed, non-shipped artifacts and neither is a framework/process path, so the narrow rules `Write(scripts/*.py)` and `Write(docs/*.md)` (excluding `docs/aide/vision.md`/`roadmap.md`, which stay PR-gated by their own entries) belong in `.claude/settings.overlay.json`'s `permissions.allow.add`, not in a per-run local grant that the next machine does not have *(item 118, 2026-08-27)* → applied 2026-08-30 — Write(scripts/*.py) and Write(docs/*.md) added to .claude/settings.overlay.json permissions.allow.add and mirrored into the generated settings.json (the repo is behind the framework, v1.21.0 vs v1.25.2, so the mirror stands in for the regeneration the next install.py --update will reproduce)
- [x] defect — an item spec's `## Authorised paths` → `Asserts against` list is checked at the file-path level regardless of what part of a file changed, so pinning `docs/aide/progress.md` there ("read only; this item makes no edit to it") is incompatible with the mandatory `aide progress set NNN in-progress` step every item's builder runs — that step always changes the file's status column, so `aide scope` reported a contradiction on item 118 even though the asserted content (the Human gates row's `Blocks` cell, AC8) never moved. `progress.md` is already covered by `_ALWAYS_AUTHORISED` for exactly this routine flip, so the extra pin was redundant and only added a false-positive failure mode; item 117's own spec already documents that an "Asserts against" contradiction is checked independent of the always-authorised list (`scope_findings`). Fixed in item 118's own spec by dropping the bullet; worth a lint or a documented convention so future specs referencing `progress.md` for a read-only content check (AC8-style) don't repeat it *(item 118, 2026-08-27)* → aide-loop#107
- [x] knowledge — `feature_docs.STATUS_OVERRIDES` is a verbatim transcript of the item-106 maintainer walkthrough, so an item that *fixes* the concern an override records has no sanctioned way to retire it: `stage3.curvature.total_curvature_deg`'s override says "Should be expressed per axis component (three values) rather than as one aggregate scalar", which item 122 partly delivers by splitting the descriptor into coronal and sagittal sweeps, yet the row still reads `retune` for the stated reason and rewriting a recorded human call from inside an item is not an agent's to do. Needs either a dated append-trail convention like `insights.md`'s, or a review pass at a queue boundary that re-asks the maintainer *(item 122, 2026-08-27)* → roadmap.md Backlog — 'STATUS_OVERRIDES has no sanctioned retirement path' (2026-08-30 triage)
- [x] defect — `stage3.curvature.tangent_angles_deg[]` is the unsigned angle to `+S` with no traversal-direction normalisation, so a centroid sequence supplied cranial-first (as real anatomy ordered by ascending label is) reads near `175°` per level where a caudal-first sequence reads near `5°` — the same spine, two readings, and no consumer can tell which it got. Never noticed because every committed fixture happens to advance superiorly. Item 122 normalises direction for the new signed per-plane arrays it adds but deliberately leaves this array alone (its scope fence is the scalar reduction), so the record now carries two tangent-angle conventions side by side. Reconciling them belongs with item 121 or Stage 20 *(item 122, 2026-08-27)* → roadmap.md Carried defects — 'tangent_angles_deg carries no traversal-direction normalisation' (2026-08-30 triage)
- [x] gap — `scipy.interpolate.make_splprep` accepts a per-point weight vector `w` that the Stage 28 curve decision does not use: it fits every centroid at equal weight. Two uses are identified and deferred in [`docs/spinal-curve-model.md`](../spinal-curve-model.md) §"Spline weights — deferred, not rejected" — down-weighting vertebrae clipped by the field of view, whose centroids are displaced toward the image interior by the crop rather than by anatomy (the per-label border-contact signal already exists, `heuristics/border.py` reads it), and up-weighting the cranial-most and caudal-most levels when they are known clear of the border, where a spline has the least support and the most influence over end behaviour. They are one scheme rather than two, because up-weighting a clipped terminal vertebra amplifies the first problem. Deferred because the weighting would itself need calibrating against real GT, and Stage 28's remit is to make the offset and orientation features carry signal at all *(item 118, 2026-08-27)* → already recorded at capture in docs/spinal-curve-model.md §'Spline weights — deferred, not rejected'; nothing further to route
- [x] gap — the Stage 28 deformity envelope is proposed as a single threshold covering all anatomy, and a refinement to separate normal and scoliotic envelopes is anticipated, so that a curve magnitude which is a finding in one population is expected anatomy in the other. That refinement is **pathology differentiation** — telling a scoliotic spine from a normal one — which is a different objective from the failure-mode detection this repo is scoped to, where the question is whether a *segmentation* is wrong rather than whether an *anatomy* is atypical. Recorded in [`docs/spinal-curve-model.md`](../spinal-curve-model.md) §"The deformity envelope is expected to be revised". Whether it belongs in FACET at all is a `vision.md` question and should be answered there before any item implements it *(item 118, 2026-08-27)* → roadmap.md Backlog — 'Is a scoliosis-vs-normal envelope FACET's to build at all?' (2026-08-30 triage); the vision.md answer itself is still owed by a person
- [x] defect — `fit_centroid_spline` (`src/segfacet/features/spline.py`) raises `ValueError: Invalid inputs.` from `scipy.interpolate.splprep` when all supplied centroids are exactly coincident (zero-length chords everywhere), rather than degrading gracefully or raising a descriptive error naming the cause; item 122 (`compute_spine_curvature` / `SpineCurvature` in `orientation.py`) does not own the spline fit and cannot exercise its degenerate-near-zero-tangent adversarial case against this crash, so `tests/test_122_signed_curvature.py::test_adv_all_centroids_coincident_no_crash_finite` uses a near-coincident (1e-6 mm) fixture instead *(item 122, 2026-08-27)* → roadmap.md Carried defects — 'fit_centroid_spline crashes uninformatively on all-coincident centroids' (2026-08-30 triage)
- [x] knowledge — `docs/aide/golden-decision-table.md` carries a human sign-off (dated 2026-07-28) alongside measured evidence cells (the `N/M leaf paths unwired` fraction) that any feature-adding item invalidates, because `M` is the live leaf-path count and `N` the live unwired count off `segfacet.catalogue.build_catalogue()`. The sign-off and the measured evidence age at different rates — the judgement (disposition, rationale, replacement guarantee) is stable across feature additions, but the count is not — yet both live in the same signed-off document, so every refresh of the count (items 106, 110, now 122) has to touch a human-signed artifact and add a paragraph explaining that no judgement moved. Separating the measured counts from the judgement columns — e.g. into a small generated companion table the signed document merely references — would let a count refresh happen without ever touching the signed text *(item 122, 2026-08-27)* → roadmap.md Backlog — 'golden-decision-table.md mixes a dated human sign-off with live measured counts' (2026-08-30 triage)
- [x] knowledge — Stage 28's acceptance line "A clean GT spine stays within item 017's 0.5 mm pass-through bound across level counts and spacings" (`docs/aide/progress.md`) is not satisfiable by the formulation the 2026-08-27 human gate approved: `docs/spinal-curve-model.md` itself records `smoothing_spline`'s in-sample clean-GT sweep maximum as 0.552139 mm, reproduced on the item-119 branch at 5 levels x (0.8, 0.8, 1.0) spacing, while item 017's own GT fixtures peak at 0.19198 mm and still pass unweakened. The two claims can both be true because they are measured on different fixture families; item 125 needs the acceptance line ticked against that measured split (or reworded) rather than against the aspiration *(item 119, 2026-08-27)* → resolved 2026-08-28 — stage 28's acceptance bound raised 0.5 -> 1.0 mm in roadmap.md, progress.md and queue-017.md, with the rationale in roadmap.md's Stage 28 acceptance note; item 017's AC1 unchanged
- [x] gap — `docs/spinal-curve-model.md` ("Breaking circularity") assigns the leave-one-out per-label evaluation to item 119, while `docs/aide/queue/queue-017.md` assigns promoting it from the test harness into the pipeline to item 120; the two are the same change, and the disagreement is visible only to a reader who opens both. A decision document that names item numbers is a second place where the work breakdown lives, and it drifts from the queue that owns it *(item 119, 2026-08-27)* → docs/spinal-curve-model.md §'Breaking circularity' now carries a dated note deferring the work breakdown to queue-017.md as the assignment of record
- [x] framework — `aide check --queue NNN`'s cross-spec authorised-path comparison does not discount items already ✅ in `progress.md`, so a completed item's spent "May change" list keeps colliding with every later spec that pins the same file. On queue 017 this reports four errors against items 118 and 122 (both merged) that no later item can clear without editing a completed item's spec — e.g. item 118 pins `src/segfacet/features/spline.py`, which item 119 exists to rewrite. Skipping ✅ items on both sides of the comparison would leave only live conflicts *(item 119, 2026-08-27)* → aide-loop#106
- [x] gap — item 119's spec authorises editing only `test_ac4_determinism_curved_spine` in `tests/test_017_centroid_spline_fit.py`, but `test_spline_fit_has_required_fields` in the same file also asserts `hasattr(fit, "tck")`, which the item removes (SplineFit.tck -> spline/smoothing). The Testing Strategy's reconciliation table does not list this test, so it is left as a genuine stale-test gap outside test-writer's authorised scope for that file rather than silently patched *(item 119, 2026-08-27)* → resolved — tests/test_017_centroid_spline_fit.py::test_spline_fit_has_required_fields was reconciled to the item-119 SplineFit shape (spline/smoothing, tck removed)
- [x] gap — a spec that changes a feature which the bundled reference artifact aggregates must survey *every* test comparing a fresh build against the committed artifact, not a hand-listed subset. Item 119's Assumptions named `test_049`/`test_090`/`test_092`/`test_097`/`test_098` and missed `test_045`/`test_063`/`test_081`, whose five "regenerate and compare to committed" tests fail by construction against a stale artifact. `grep -l build_and_write_default tests/` finds them mechanically; a lint over specs that pin `reference_*.json` as read-only while touching an `INGESTED_FEATURES` feature would find them earlier *(item 119, 2026-08-27)* → roadmap.md Carried defects — 'Committed-artifact comparisons keep being re-authored byte-exact', which carries the mechanical-survey rule (2026-08-30 triage)
- [x] knowledge — `scipy.interpolate.make_splprep`'s returned `BSpline` evaluates **dimension-first**: `spl(u)` and `spl(u, nu=1)` are `(n_dim, N)`, not the `(N, n_dim)` that the legacy `splev`-plus-`column_stack` idiom produced. The two layouts are indistinguishable whenever `N == n_dim`, so any check of a 3-D curve at exactly 3 parameter values silently passes under either. Verify shape contracts at a disambiguating `N` *(item 119, 2026-08-27)* → the fact lives in the owning code — src/segfacet/features/spline.py's evaluation helpers document the (3, N) -> (N, 3) transpose contract at the call sites
- [x] defect — the human gate "Spinal curve model" (`docs/aide/progress.md`, approved 2026-08-27) records `max_offset_mm` raised `15.0 -> 25.0`, justified by a `21.073357` mm leave-one-out ceiling across VerSe19 GT. That ceiling was measured by `scripts/compare_curve_candidates.py` over **interior levels only** (`range(1, len(centroids) - 1)`), because dropping a terminal level truncates the curve's parameter domain and turns the measurement into extrapolation. A pipeline feature must value every level, so item 120 ships a domain-preserving estimator instead, under which `mode1_displace`'s displaced label reads `18.719` mm — below 25.0, so adopting the gate's figure would stop the corpus's own mode-1 case firing. An approved threshold and the estimator it was calibrated against have to travel together; item 123 must re-measure the real-GT ceiling under the shipped estimator before moving the threshold *(item 120, 2026-08-28)* → superseded — item 123 re-measured under the shipped estimator and recalibrated interior-only to 13.0 mm; the gate row's supersession is recorded in progress.md (commit 33ffa28) and docs/reference-build.md's 2026-08-29 rebuild records
- [x] gap — roadmap Stage 28 asserts "a smoothed fit detects the mode-4 swap directly (`non_monotonic_pairs=(('L2','L3'),)`)", and stage acceptance requires `is_monotonic` to be `False` on `mode4_relabel_swap`, but no item in `queue-017.md` owns making that true: 119 changes the fit, 120 the offset evaluation, 121 orientation, 122 curvature, 123 regeneration, 124 the catalogue column. Measured on the shipped item-119 fit, `is_monotonic` is still `True` on all nine corpus cases including mode 4, so the premise did not hold and `mode4_relabel_swap` stays a `reconstructed_record` case. Item 125 would have to record the criterion unmet, or the stage needs an item that owns `features/consistency.py` *(item 120, 2026-08-28)* → roadmap.md Carried defects — 'Stage 28's mode-4 acceptance premise has no owner' (2026-08-30 triage)
- [x] defect — item 120's AC26 ("The regeneration moves no verdict... For all nine regenerated goldens, verdict is unchanged from its pre-120 committed value") reads as contradicting its own AC18/AC20, which require `mode1_displace` to fire `mislabel` through plain `run_qc` and lose its `reconstructed_record` status. Since verdict aggregation is severity-dominance (`aggregate.py`) and `mode1_displace`'s pre-120 committed golden verdict is `pass` with zero findings, a genuine `mislabel` finding necessarily moves it to `flagged-for-review` — one case's verdict does change. `tests/test_120_leave_one_out_offset.py::test_ac26_regeneration_moves_no_verdict_outside_mode1s_own_deliverable` asserts the coherent reading instead (verdict unchanged for the other eight cases; `mode1_displace` moves to `flagged-for-review` as AC18 requires) *(item 120, 2026-08-28)* → resolved in-item — tests/test_120_leave_one_out_offset.py::test_ac26_regeneration_moves_no_verdict_outside_mode1s_own_deliverable asserts the coherent reading
- [x] defect — `pyproject.toml` and `constraints.txt` pin `tptbox==0.7.5`, whose **published package metadata declares AGPL v3.0** (`pip show TPTBox` reports it), while TPTBox's own `LICENSE` file is Apache-2.0. Upstream resolved that contradiction in favour of Apache-2.0 in [PR #119](https://github.com/Hendrik-code/TPTBox/pull/119), first released in v0.7.6, so every release from v0.7.6 on carries correct metadata and 0.7.5 does not. Anything that reads dependency licences off installed metadata — an SBOM, a compliance scan, a downstream redistributor — will currently see this project pulling in an AGPL dependency. Bumping the pin is a dependency change with its own regression surface (the golden corpus), so it needs an item rather than a drive-by edit *(2026-08-28)* → roadmap.md Carried defects — 'The tptbox==0.7.5 pin ships wrong licence metadata' (2026-08-30 triage)
- [x] defect — item 120's Testing Strategy table names `tests/test_110_neighbourhood_wiring.py` only for its `offset_mm` mean/median/std aggregation ("no change expected if structural"), but that file also carries `test_ac11_corpus_findings_rule_ids_unchanged`, which asserts `{f.rule_id for f in pipeline_findings(case)} == set(case["expected_rule_ids"])` for every `detection == "pipeline"` corpus case. AC23 deliberately adds a `mislabel` finding on `mode6_crop_at_border` through plain `run_qc` while its `expected_rule_ids` stays `["border"]` (only the *designated* rule, per AC23's own wording) — so this pinned equality now fails for that one case. `tests/test_110_neighbourhood_wiring.py` is item 110's (✅ merged) and is not in item 120's Authorised paths, so it was left unedited rather than patched out of scope; the full suite will show this one failure until a later item widens the assertion to subset (`>=`) or an authorised follow-up touches item 110's test file *(item 120, 2026-08-28)* → resolved — tests/test_110_neighbourhood_wiring.py's pinned equality was widened to the subset assertion (expected_rule_ids <= fired rule_ids) during queue-017's CI reconciliation
- [x] gap — `compute_leave_one_out_spline_offsets` (`src/segfacet/features/spline_offset.py`, item 120) withholds only the single dominant outlier per refit; with two or more genuinely displaced levels every un-withheld displaced level still pulls each other held-out curve toward itself. Measured on the adversarial two-opposite-displacements case: a genuinely clean level reads 31.96 mm while one of the displaced levels reads only 19.31 mm, so a clean vertebra can be named an offender ahead of an actual one. Shipped as a known, documented limitation by owner decision rather than fixed here; the natural follow-up is withholding every level above some outlier cutoff, not just the single dominant one *(item 120, 2026-08-28)* → roadmap.md Backlog — 'Held-out offset estimator: two known blind spots, deferred by owner decision' (2026-08-30 triage)
- [x] gap — the closest-point-on-spline search exists three times with the same coarse-scan-plus-`minimize_scalar` algorithm and no link between them: `_find_closest_u` in `src/segfacet/features/spline_offset.py:191` (backend-aware, `_N_SCAN = 500`), `_find_closest_u` in `src/segfacet/features/consistency.py:128` (no backend parameter), and a third re-implementation in `scripts/compare_curve_candidates.py:211`, whose docstring names the spline_offset copy it duplicates. A change to the scan resolution, the refinement bracket or `xatol` has to be made in three places, and a divergence between them would show up as an inconsistency between `per_label_offsets[].closest_u` and `monotonic_consistency.u_values[]` rather than as a test failure. Item 121 reached the closest point through the public `compute_spline_offsets` rather than adding a fourth caller *(item 121, 2026-08-29)* → roadmap.md Carried defects — 'The closest-point-on-spline search exists three times…' (2026-08-30 triage)
- [x] defect — `scripts/refresh_reference.py --verse-cohort <real VerSe root>` (item 083) cannot rebuild the real artifact: it passes the cohort directory straight to `build_reference`, whose `ingest_cohort` (item 044) lists **one** directory with `os.listdir` non-recursively and hardcodes each mask's CT sibling as `<id>_scan.nii.gz`, while VerSe nests masks under `derivatives/sub-verseNNN/` and names CTs `..._ct.nii.gz`. The wrapper records `verse-build: failed` rather than staging, so item 083's "one-command refresh" has never actually produced `reference_verse_v1.json` from the real cohort; `docs/reference-build.md` covers the gap with a manual bash staging snippet. Item 123 ships staging in a dedicated tool rather than widening item 083's contract *(item 123, 2026-08-29)* → roadmap.md Carried defects — 'Item 083's one-command refresh has never rebuilt the real artifact' (2026-08-30 triage); the working rebuild is item 123's scripts/rebuild_verse_reference.py
- [x] gap — `compute_leave_one_out_spline_offsets` (item 120) returns `offset_mm == 0.0` for **every** level at exactly 4 centroids: its `< 4` in-sample fallback boundary is one level too low, because at `k = min(3, n - 1) = 3` with four points the B-spline has as many coefficients as points and the smoothing term has no freedom to act, so the held-out refit interpolates. Measured on real VerSe19 GT (`sub-verse065`, 4 labelled levels, every offset `0.000` mm) — the same mechanism item 119's Decisions log records for its `_balanced_s_curve` fixture. A 4-level field of view therefore silently cannot raise a `mislabel` offset finding *(item 123, 2026-08-29)* → roadmap.md Carried defects — 'At exactly 4 centroids the held-out estimator silently reads 0.0 everywhere' (2026-08-30 triage)
- [x] framework — `aide check --queue NNN` reads every item number appearing **before** a Dependencies section's `**Downstream` marker as a blocker, including numbers that are merely quoted from a human gate row's `Blocks` cell. Items 119, 120 and 121 each transcribe `Blocks: 119, 120, 121, 123, 125` into their Dependencies section, so each is read as depending on item 123; once item 123's spec names 120 as its (genuine, already ✅) blocker, the check reports `dependency cycle 120 -> 123 -> 120` and `120 -> 123 -> 121 -> 120`. Both are inert — every item in them is ✅ — but the cycle check does not discount completed items, and quoting a gate's reach is the natural way to write that bullet. Either the cycle check should skip ✅ items, or a gate row's `Blocks` list should not be parsed as a dependency edge *(item 123, 2026-08-29)* → aide-loop#106
- [x] gap — Item 123's real-VerSe19 rebuild (80/80 subjects, item-120 held-out estimator) measured the `mislabel.max_offset_mm` real-GT ceiling `P = 21.209073949050172` mm at level `L5` (`count = 62`) — clear of every other level's `p99` (next-highest `T10` at `14.24` mm) and outside the 2026-08-27 gate's approved corpus window `(5.143859, 17.507445]`. Item 123 hands this back per its own spec rather than clamping (see its Decisions log and `docs/reference-build.md`'s 2026-08-29 rebuild record); whoever revisits the deformity-envelope gate should first determine whether `L5`'s reading is genuine severe lumbar curvature in this cohort, a lumbosacral-transitional-vertebra boundary effect, or an artefact of item 120's held-out estimator, before deciding whether to widen the envelope, exclude `L5` from the qualifying-level sweep, or revise the derivation rule. The top-offset subject ids (`sub-verse015` highest) are recorded in the rebuild's `verse_rebuild_summary.json` *(item 123, 2026-08-29)*
  - Resolved same day: the follow-up per-vertebra analysis (see the two entries below) found the reading was entirely terminal-extrapolation, not deformity. Human decision excluded sequence-terminal vertebrae from the rule and the reference distribution; the re-run interior-only ceiling is `12.90577608928562` mm at `T10`, giving the shipped threshold `13.0` mm, inside the approved window *(item 123, 2026-08-29)*
- [x] gap — **deferred, by design:** excluding sequence-terminal vertebrae from the `mislabel` offset detector and from `spline_offset_mm`'s reference distribution (item 123, human decision 2026-08-29) is a stop-gap, not a model: a genuinely displaced cranial-most or caudal-most vertebra is now not detected at all, because it is no longer looked at. A terminal vertebra's offset is un-judgeable *by item 120's held-out estimator* — the refit must extrapolate past the end of its own parameter domain — not meaningless. Measured on VerSe19: terminals are 160/863 occurrences (18.5%) but 41/45 of the >= 6 mm outliers (91%), and 7 of 8 at >= 15 mm (6 caudal); L5 reads median 4.55 / p99 21.49 / max 26.87 mm when caudal-terminal against a 1.00 mm interior maximum. The real treatments are a separately calibrated terminal threshold, an extrapolation-aware estimator, or a curvature model that does not need both neighbours *(item 123, 2026-08-29)* → roadmap.md Backlog — 'Held-out offset estimator: two known blind spots, deferred by owner decision' (2026-08-30 triage)
- [x] knowledge — `sub-verse406_split-verse261`'s **interior** T10 reads 18.51 mm held-out offset, the largest interior reading in the VerSe19 training cohort and the single value that sets item 123's calibrated `max_offset_mm` (it drives T10's interior p99 of 12.91 mm, from which 13.0 mm is derived). Unlike the terminal outliers it is not an extrapolation artefact, so it is either genuine anatomy — a real deformity the envelope must accommodate — or a segmentation/labelling error in the ground truth, and which it is changes whether 13.0 mm is calibrated on signal or on a bad case. Worth inspecting directly before the threshold is treated as settled *(item 123, 2026-08-29)* → roadmap.md Backlog — 'Adjudicate sub-verse406_split-verse261 T10 before treating max_offset_mm = 13.0 as settled' (2026-08-30 triage)
- [x] knowledge — the pre-123 (2026-07-17) committed `reference_verse_v1.json` had **incomplete intensity coverage**: every level's `intensity_*` statistics were computed from a strict subset of that level's subjects (e.g. `L1`: 47 of 59; `C1`-`C4`: 1 of 13), not all 80. Item 123's rebuild tool finds a CT for every one of the 80 subjects via recursive, split-infix-aware discovery, so the rebuilt artifact's intensity coverage is complete (count == record_count on every level) — a legitimate improvement, verified not to be a scan/mask mispairing (zero geometric features moved on any level; spot-checked HU ranges are physiologically plausible). The likely historical cause: `docs/reference-build.md`'s pre-123 hand-staging recipe iterated over each split-subject's *parent* directory and copied `${sub}_ct.nii.gz`, which cannot name a split subject's own `${sub}_split-verseMMM_ct.nii.gz` file — not verifiable after the fact (no July staging logs), but consistent with the deficit concentrating among cervical/upper-thoracic levels. Any other item that measures or re-derives thresholds from `reference_verse_v1.json`'s `intensity_*` statistics (e.g. a future item touching `heuristics/intensity_reference_delta.py`'s defaults) should know the pre-123 baseline it might be diffing against undercounted every level *(item 123, 2026-08-29)* → folded into docs/reference-build.md's 2026-08-29 rebuild record ('Intensity coverage changed with this rebuild')
- [x] gap — `out/` is not in `.gitignore`, yet it is the `--out` target in the reproduction commands the repo itself documents (`docs/spinal-curve-model.md`'s `scripts/compare_curve_candidates.py --out out/curve-candidates`, and `docs/reference-build.md`'s rebuild recipe). Following either recipe leaves an untracked `out/` behind, after which every subsequent `python .aide/scripts/aide.py sync --item NNN` refuses with "working tree not clean -- commit or stash before starting: ?? out/" and the session's deterministic preflight stalls until someone hand-removes it. Encountered live while starting item 125 (leftovers from item 123's VerSe rebuild, dated 2026-08-29). Either `.gitignore` should cover `out/` or the documented recipes should target a scratch path outside the repo *(item 125, 2026-08-30)* → fixed — out/ added to .gitignore at the 2026-08-30 feedback-loop triage
- [x] framework — `aide check --queue NNN`'s pin-vs-edit check (`item A may change X, which item B pins as X under Asserts against`) does not discount items that are already ✅ and merged, so it fires structurally on **every** stage-validation item. A `Validate stage N` item exists precisely to pin the artifacts its stage's items produced, so its `Asserts against` list necessarily collides with their `May change` lists — item 125 draws 14 such errors from items 118-123, all inert, and neither remedy the message offers is correct (widening the pin drops the artifacts the item exists to observe; narrowing the earlier edits is impossible, they are shipped). Same root cause as the dependency-cycle entry above (2026-08-29): the cross-spec checks reason over spec declarations without consulting `progress.md` status. A completed item can no longer "land" after anything *(item 125, 2026-08-30)* → aide-loop#106
- [x] defect — `docs/spinal-curve-model.md`'s "Revisions to apply when item 119 implements this" section claims that switching the `smoothing_spline` candidate from raw `splprep` to the shipped `fit_centroid_spline` (`make_splprep`, identical `s = n_points`) leaves "every value in `## Measurements` still reproduces" — measured false for one row. Re-running `scripts/compare_curve_candidates.py --verse-cohort dataset-verse19training` on 2026-08-30 reproduced 15 of the 16 documented keys exactly (within the stated 0.001 mm tolerance, including the count-valued determinism row), but `candidates.smoothing_spline.verse_scoliotic.max_pass_through_mm.leave_one_out` measured `20.683092` mm against the documented `21.073357` mm — a 0.390 mm divergence, ~400x the stated tolerance. `make_splprep` is SciPy's newer, independent smoothing-spline implementation (not a `splprep` wrapper with the same numerics), so an identical `s` value does not guarantee an identical fit for every input; this particular leave-one-out fit over one of the 17 selected real scoliotic cases is evidently sensitive to that difference even though every other measured key (including three other `verse_scoliotic` rows on the same 17-case selection) reproduced to within 1e-6 mm. Immaterial to the shipped `max_offset_mm = 13.0` (item 123 superseded the original 25.0 mm envelope this figure fed with its own, differently-derived interior-only ceiling), but the document's specific "still reproduces" claim is not true and should be corrected or re-measured rather than cited as-is *(item 125, 2026-08-30)* → docs/spinal-curve-model.md §'Revisions to apply…' now carries the dated 2026-08-30 correction with the measured divergence
- [x] defect — `segfacet run`'s **default** invocation (no `--reference`/`--no-reference` flag) enables reference mode against the bundled real-VerSe19 `reference_verse_v1.json` (item 090's default), so a real end-to-end CLI run on any of the corpus's tiny synthetic box fixtures (30x25x25 mm vertebrae, versus real anatomy) reports dozens of `bounds` and `reference_delta` findings and a `flagged-for-review` verdict even for `clean_control_seg.nii.gz` — not the "zero findings, pass verdict" the corpus/stage documentation describes for a clean case. Measured 2026-08-30: `segfacet run --scan tests/corpus/fixtures/base_scan.nii.gz --seg tests/corpus/fixtures/clean_control_seg.nii.gz --out <dir>` (no flags) emits ~40 findings; the same command with `--no-reference` reproduces the expected zero-findings/pass result, matching plain `run_qc(bundled_default_config())`. Anyone reproducing a stage's "clean control fires nothing end-to-end" claim via the bare CLI (rather than `--no-reference` or the test harness) will see this and may mistake it for a regression; it is item 090's documented default behaviour operating on a fixture it was never calibrated against *(item 125, 2026-08-30)* → folded into CLAUDE.md Gotchas (a bare segfacet run defaults to the real-VerSe19 reference; corpus-fixture reproductions need --no-reference)
- [x] gap — **G3 acceptance not met, measured directly against the shipped pipeline.** Running every one of the 17 real VerSe19 subjects the decision document's scoliosis-selection rule (`coronal_deviation_mm >= 8.0` mm) selects through the shipped `run_qc` with `bundled_default_config()` (item 125, 2026-08-30): 1 of 17 fires a genuine `mislabel` offset finding — `sub-verse406_split-verse261`, label 17 (T10), `offset_mm = 18.51028119357566` against the shipped `max_offset_mm = 13.0` threshold. This is the same subject/level item 123 already identified as the single value that calibrated `13.0` mm in the first place (`insights.md`, item 123, 2026-08-29 entries), now confirmed to actually trip the rule end-to-end rather than merely sit near the threshold. Whether `18.51` mm is genuine coronal/kyphotic anatomy the envelope must accommodate or a GT labelling artefact (already flagged as worth inspecting directly, item 123) is unresolved; either way Stage 28's G3 acceptance box ("a real scoliotic curve in the VerSe cohort is not flagged as an offset outlier") stays unticked until it is *(item 125, 2026-08-30)* → roadmap.md Backlog — 'Adjudicate sub-verse406_split-verse261 T10…' (2026-08-30 triage), which records that Stage 28's G3 box stays open until the case is adjudicated
- [x] knowledge — The pipeline refits the identical in-sample centroid spline twice per case: `pipeline.py`'s curvature/tangent fit and `compute_leave_one_out_spline_offsets`'s internal reference refit at `features/spline_offset.py:444`; harmless but redundant, noted at the queue-017 pre-PR review *(items 119-123, 2026-08-30)* → roadmap.md Carried defects — 'The closest-point-on-spline search exists three times, and the pipeline refits the same spline twice per case' (2026-08-30 triage)
- [x] knowledge — three item-120/121/123 tests asserted byte/exact identity between a freshly-computed value and a **committed** artifact (`reference_default.json`, a golden's PCA `principal_axis`/`eigenvalue_ratio`) rather than going through the numeric-tolerance pattern item 078 established (`reports_close`; CLAUDE.md "Note what the golden tests actually assert") — the same defect class the convention exists to prevent, reintroduced in three separate items' own test-writing passes rather than in any src/ change. All three passed locally and on ubuntu-latest and were only caught by PR #56's CI matrix (numpy 1.26.4, numpy 2.0.2, windows-latest), where committed floats differ by ~1 ULP. Suggests the convention needs a lint or a shared fixture/helper name that makes "comparing against a committed artifact" reach for tolerance by construction, rather than relying on each item's test-writer to recall the rule *(items 120-123, 2026-08-30)* → roadmap.md Carried defects — 'Committed-artifact comparisons keep being re-authored byte-exact' (2026-08-30 triage), which proposes the shared helper + guard test and names the golden-retirement pull-forward
- [x] knowledge — a byte-compared generated artifact (item 103/124's `feature_catalogue.generated.{json,md}`) can stay flaky under 6g quantisation even after item 078's tolerance pattern is followed everywhere else: item 124's `observed.corpus` block embedded raw measurements from structurally-constant, sub-`NEGLIGIBLE_MAGNITUDE` paths (e.g. `magnitude=3.66317e-14`) whose exact bits are NumPy/SciPy cancellation-scale noise that differs across NumPy builds and CPU microarchitectures — `float(f"{v:.6g}")` cannot stabilise a value that is noise rather than signal, since noise quantised to six digits is still noise. PR #56's CI matrix failed the five byte-exact regeneration test modules (103/106/119/120/122; 8 tests) on one numpy-1.26.4 run and passed them on another with no code change. Fixed by clamping a covered-but-not-`informative` population's emitted numeric fields to `0.0` at the serialisation boundary only (`segfacet.observed_range.emission_range`, called from `catalogue.py`'s serialisers, never from `build_observed_ranges` itself) rather than by further quantisation. Any future byte-compared artifact that reports a raw float measurement alongside its own "is this value meaningfully nonzero" threshold should clamp sub-threshold noise to a fixed sentinel at emission, not merely round it *(item 124, 2026-08-30)* → roadmap.md Stage 29 D2 (tolerance by construction) carries the emission-clamp rule for artifacts that legitimately stay byte-compared (2026-08-30 triage)
- [x] defect — `tests/test_116_ras_native_corpus.py`'s AC7 case-identity fence resolves its pre-migration reference by probing candidate refs for `tests/corpus/golden/clean_control.json` at the merge base (`_merge_base_sha`), and is marked `skipif(_MERGE_BASE_SHA is None)`. Item 126 retires those snapshots, so the probe keeps succeeding only while the merge base predates the retirement: once it reaches `main` and the base advances, all nine parametrised cases silently degrade to a skip rather than failing. The fence is not broken by the deletion itself (it reads git history, not the working tree) and item 126 leaves it untouched; making it durable needs a decision — pin the last pre-retirement commit as a constant, or delete the fence as discharged *(item 126, 2026-08-30)* → commit 9ef7116 on aide/queue-018 — tests/test_116_ras_native_corpus.py now pins _REFERENCE_GOLDEN_SHA = aeb2f55 (last main commit before queue-017 landed) and reads the goldens from git history, so item 126's deletion cannot turn the nine AC7 cases into a silent skip
- [x] knowledge — `docs/aide/golden-decision-table.md`'s `tests/golden/022_stage3_report.json` row describes the consuming test's write-and-skip behaviour as "a logged, unfixed defect", but item 111 fixed it (`tests/test_111_golden_guard.py::test_ac5_no_self_healing_branch_in_test_ac8` now forbids both `pytest.skip` and a write inside that function). The cell is signed maintainer text and cannot be corrected in place, so the stale clause survives verbatim; item 126 records the correction in the table's retirement execution log instead *(item 126, 2026-08-30)* → docs/aide/golden-decision-table.md 'Retirement execution log' — item 126 recorded the 022-row correction there
- [x] knowledge — after item 126 retires the eleven snapshot goldens, two process-owned documents name paths that no longer exist and cannot be corrected from inside a work item: `CLAUDE.md`'s "Note what the golden tests actually assert" paragraph (which explains `reports_close` against the committed corpus goldens) and `.aide/conventions.md`'s `tests/corpus/golden` mention. Both are PR-gated framework/process files and need a reviewed PR rather than an item edit *(item 126, 2026-08-30)* → PR #59 rewrote CLAUDE.md's golden paragraphs for the post-126 regime; the .aide/conventions mention is an illustrative example in framework-owned text, not a claim about this repo — left as is
- [x] gap — `golden-decision-table.md`'s `asserted by` column is hand-maintained and has gone stale in the one direction nothing checks: `tests/test_105_golden_decision_table.py`'s AC6 verifies every named test *exists*, but nothing verifies that every test actually consuming a fixture is *named*. Measured 2026-08-30, an AST sweep of `tests/` for `GOLDEN_DIR`/`load_golden`/`read_golden_text`/`check_case_golden` found twelve consuming modules against the six the table lists — items 106, 108, 116, 119, 120, 121, 122 and 123 each added a consumer without a table edit, so the golden retirement's blast radius was three times the queue's estimate. A completeness check in the other direction (or generating the column) would have surfaced this at authoring time rather than at execution time *(item 126, 2026-08-30)* → roadmap.md Backlog — 'Mechanically verify the golden decision table's asserted by column in both directions' (2026-09-01 triage)
- [x] gap — the AST sweep behind item 126's disposition table (`GOLDEN_DIR`/`load_golden`/`read_golden_text`/`check_case_golden`) still missed a third category of consumer: `tests/test_119_curve_formulation.py::test_ac19_stage3_report_golden_matches_test_022_output`, `tests/test_120_leave_one_out_offset.py::test_ac27_stage3_report_golden_matches_test_022_output` / `::test_ac27_stage3_report_golden_offset_entries_carry_is_terminal`, and `tests/test_123_recalibrate_and_regenerate.py`'s same-named pair each read the 022 golden only indirectly, via `test_022_stage3_serialisation.py`'s own `GOLDEN_PATH` module attribute -- no marker string from the sweep's list appears in their source, so a text/AST-marker sweep is structurally blind to a consumer that piggybacks on another module's already-discovered golden reference. Item 126 discharged all five as duplicates of test_022's own (re-pointed) comparison once `GOLDEN_PATH` started naming the shared `report_format_contract.json` fixture instead of a per-module snapshot. A future fixture-retirement sweep should also grep for `\.GOLDEN_PATH\b` (and similar shared-attribute idioms) across the whole tree, not just the producing module *(item 126, 2026-08-31)* → roadmap.md Backlog — 'Mechanically verify the golden decision table's asserted by column in both directions' (2026-09-01 triage)
- [x] framework — an item spec that pins `docs/aide/progress.md` under its own Asserts-against list (a content-scope statement, e.g. item 126 Description: "does not touch docs/aide/progress.md") fails `aide scope` the moment its builder runs the mandatory `progress set NNN in-progress` step, because that status-tracking write touches the same file the spec declared unchanged. Measured on item 126 (2026-08-31): the only item spec in the tree with this pin, and the only one to trip it. `aide scope` has no way to distinguish a status-tracking write (always allowed) from a content edit (what the pin actually means), so either the CLI should exempt its own status writes from the diff it compares, or spec-authoring guidance should warn against pinning progress.md under Asserts-against at all *(item 126, 2026-08-31)* → resolved upstream — engine 1.23.0 (aide-loop #90) warns at spec time when a spec pins an always-authorised path; installed here by PR #59, and aide check now names items 126/132
- [x] defect — `tests/test_116_ras_native_corpus.py::test_ac7_case_identity_preserved_vs_merge_base` fails on `[mode1_displace]` and `[mode6_crop_at_border]` on `origin/aide/queue-018` (the item-126 base branch, before any of item 126's commits): the freshly-built report for `mode1_displace` no longer carries the `('mislabel', (22,))` finding item 120's committed golden documents, and `mode6_crop_at_border`'s fresh report is missing the same pair. Pipeline/mislabel behaviour has drifted from the merge-base golden independently of item 126 (confirmed by reproducing the failure in a clean worktree checked out at `origin/aide/queue-018`) — likely `main`'s recent `fix(124): clamp sub-floor noise to 0.0 at catalogue emission (PR #56 CI matrix)` shifting the mislabel margin. Needs triage by whoever owns Stage 20/23's mislabel calibration *(item 126, 2026-08-31)* → commit 9ef7116 on aide/queue-018. Diagnosis corrected: not mislabel-calibration drift — the test stripped item 120's ('mislabel', (22,)) pair from the fresh side on the assumption that the merge base predates item 120, which stopped holding when PR #56 put queue-017 on main (measured 2026-08-31: 0 mislabel findings in mode1_displace's golden at aeb2f55, 1 at fda97b0). Fixed by pinning the reference golden commit; nothing in mislabel or fix(124) moved
- [x] framework — `aide check --queue NNN`'s cross-spec `changes-pinned-state` check compares every item in the queue regardless of whether it has already merged, so a landed item's `May change` entry raises a hard error against a later item that legitimately pins the same file. Measured 2026-08-31 while specing item 127: item 126 (✅, merged into `aide/queue-018`) declares `.gitattributes` under May change, so item 127 pinning `.gitattributes` under Asserts-against reported "item 127's assertion breaks when item 126 lands" for a landing that already happened, and the pin had to be moved into prose to keep the queue check green. Skipping items whose status is ✅ would remove the false positive *(item 127, 2026-08-31)* → resolved upstream — engine 1.23.0 discounts ✅/❌ items from the cross-spec checks (aide-loop #91), extended by 1.28.1 (#106); installed here by PR #59
- [x] gap — `tests/test_082_verse_build_recipe.py::test_adv_determinism_two_builds_produce_equal_parsed_artifacts` compares two same-platform `build_reference` runs with `reports_close` numeric tolerance, justified in-comment by `eigenvalue_ratio` being "a platform-sensitive PCA float". That justification does not apply to two runs on the same platform: every sibling regeneration test (`test_063`, `test_081`, `test_120`, `test_123`) asserts byte-identity for two fresh in-process builds and only drops to tolerance for the fresh-vs-committed comparison. As written, a genuine run-to-run nondeterminism in the `segfacet build-reference` CLI path would pass unnoticed. Item 127 leaves it alone because it is a fresh-vs-fresh comparison and outside that item's committed-artifact scope *(item 127, 2026-08-31)* → roadmap.md Backlog — 'test_082 … compares two same-platform builds with numeric tolerance' (2026-09-01 triage)
- [x] gap — item 127's Validation section replay recipe does not reproduce the described failure as literally written: step 1's scratch comparison `assert dest.read_bytes() == default_artifact_path().read_bytes()` uses a function-call operand, which `committed_artifact_guard.classify_module`'s own documented resolution rules (string literal / `Path(__file__)`-chain / local variable derived from one, per the spec's Assumptions) cannot resolve — the guard stays green with this exact scratch file. Substituting a literal `Path("src/segfacet/reference/reference_default.json")` operand (the shape AC16's own synthetic test uses) reproduces the described failure correctly (validated 2026-08-31 during item 127's round-2 validation). Step 2's `-k allowlist` filter also matches no test — the real enforcement test is `test_committed_artifact_guard_reports_zero_violations`, unfiltered. Neither affects the classifier's correctness (AC15-AC19 already pin the real behaviour); only the spec's Validation-section prose needs correcting *(item 127, 2026-08-31)* → docs/aide/items/127-tolerance-by-construction-for-committed.md Decisions & Trade-offs, dated correction 2026-09-01
- [x] knowledge — `docs/aide/progress.md`'s Stage 26 acceptance box "No `_PRE_NNN_*` byte-hash fence remains, and the diff-based scope check that replaces it flags a deliberately out-of-scope edit on a scratch branch (**G7**)" keys on an identifier *naming pattern* rather than on the mechanism, so item 128's rename of `_PRE_098_REFERENCE_VERSE_V1_SHA256` to `_RELEASED_REFERENCE_VERSE_V1_SHA256` removes the last `_PRE_NNN_*`-shaped name from `tests/` while leaving the digest-literal fence fully intact — a future reader could tick the box on a rename alone. The mechanism-based check (`tests/test_115_stage26_validation.py::test_ac8_no_hardcoded_literal_fence_remains`, which classifies by AST shape and caps the corpus at one fence) is the honest reading and still reports one. Item 128 leaves the box untouched; the wording should be rekeyed to the mechanism, or the box closed with the reason the one surviving fence is legitimate (`reference_verse_v1.json` is not regenerable in CI, so its digest is the only guard) *(item 128, 2026-08-31)* → progress.md Stage 26 acceptance box, dated annotation 2026-09-01 — judged by the mechanism, not the name
- [x] framework — engine 1.21.0's `cli_subprocess_test_warnings` lint (conventions.md §6, "prefer calling the function over shelling out") has no exemption for a checking test whose whole job is to replay the CLI's own output, so any item spec whose Validation section asks a committed test to assert on `python .aide/scripts/aide.py check`'s literal stdout (as item 128's AC23 and `tests/test_128_relocation_checks.py::test_ac23_aide_check_reports_the_same_three_baseline_warnings` both do) makes `aide check` warn about that very test module — inflating its own warning count by exactly one and falsifying any hardcoded baseline the spec recorded before the checking test existed. Measured 2026-08-31 on item 128: base commit `2e9c20e` (already carrying the committed checking module) reports 4 warnings, not the 3 the spec's Assumptions block recorded; the 4th is the checking module's own AC23 test flagged by this lint. No other test module under `tests/` currently trips this rule. Either the lint should exempt a subprocess call whose asserted string is itself the object under test (self-referential replay), or spec-authoring guidance should warn against pinning an exact `aide check` warning count from a checking test that itself shells out to `aide.py` *(item 128, 2026-08-31)* → aide-loop#123
  - **2026-09-01** (pending handover) → aide-loop issue drafted at the 2026-09-01 feedback-loop triage, not yet filed — filing needs a human to confirm; the draft names engine 1.21.0 as the observed version
  - **2026-09-02** → resolved upstream in engine 1.29.2, landed here with the 1.34.0 install. The proposed lint exemption was **declined**: `cmd_check` calls `run_checks`, which returns `(errors, warnings)` as structured data, so asserting in-process is both the fix and the better test, and exempting the shape would license the worse test where the argument for it sounds strongest. §6 instead carries the positive rule this entry's second option asked for — never pin an exact warning or error count from a module that itself trips the lint being counted; match the warning you mean. This repo is already on that side: `tests/test_128_relocation_checks.py` calls `run_checks` in-process and `aide check` reports no `shells out to aide.py` warning.
- [x] defect — `tests/test_128_relocation_checks.py::test_ac23_aide_check_reports_no_warning_beyond_the_pinned_baseline` pins an exhaustive allowlist of `aide check` warnings (the 32-spec Assumptions audit note and the two Stage-16 gate notices) but has no allowance for `aide check`'s own "stale claim branch" warning, which fires legitimately for exactly the item-128 branch during the very window `aide merge`'s post-merge re-test runs in: under `auto-merge`, `aide merge` merges, marks the item ✅, re-runs the full suite as a gate, and only deletes the now-merged claim branch *after* that gate passes. So the merge itself manufactures the one warning this test cannot tolerate, and the branch cleanup that would silence it is exactly what a red gate blocks — a catch-22 first hit during item 128's own round-2 validation on 2026-08-31: `aide merge 128` merged and set the item done, but its post-merge suite run failed on this assertion with `['stale claim branch aide/128-relocate-the-reference-verse-v1: item 128 is already ✅']`. Recovered by running `aide gc --merged --base aide/queue-018 --yes` by hand to delete the already-merged branch, then re-running the full suite green (6291 passed). The test should tolerate a transient stale-claim-branch warning for the item under test (or the item-under-test's own branch specifically), not just the two fixed categories it currently allows *(item 128, 2026-08-31)* → commit 6bb0b96 on aide/queue-018 — test renamed test_ac23_aide_check_emits_no_gitattributes_lint_warning and narrowed to AC23's actual claim (no .gitattributes fixture-pin lint warning, none naming the artifact or the relocated module); it no longer pins the total warning set, so aide merge's transient stale-claim-branch warning cannot redden a post-merge re-test. test_114's AC8 already tolerated that category by prefix
- [x] knowledge — Stage 29 D5's prescribed mechanism cannot produce its own stated acceptance, and no boundary value can. `roadmap.md`'s Stage 29 acceptance and queue-018's item-129 *Testable* line both require "a 4-level field of view yields non-degenerate held-out offsets" from moving `features/spline_offset.py`'s `_MIN_LEVELS_FOR_HELD_OUT` from 4 to 5, and expect four-level VerSe subjects to move "from zero to a real offset" (hence a reference rebuild and a `max_offset_mm` re-check). Measured 2026-08-31 while specing item 129: with four points and `k = 3` the fit is a cubic with four coefficients, so it interpolates all four whatever the weights are — held-out and in-sample offsets agree to ~1e-13 mm at n=4, and an interior level displaced a full 15 mm reads `0.0` mm under both (at n=5 it reads 5.58 mm held-out, at n=6 15.00 mm). The boundary move is therefore an honesty fix with no numeric consequence: no reference artifact, corpus value or threshold moves. Closing the gap needs the fit's degree clamped below `n-1` at small `n`, which changes the formulation the 2026-08-27 "Spinal curve model — the deformity envelope" gate approved. Item 129 lands the boundary move and leaves the acceptance line for item 135 to record unmet with this evidence, Stage 28's precedent *(item 129, 2026-08-31)* → progress.md Stage 29 acceptance — item 135 recorded the four-level clause unmet with this evidence
- [x] gap — a segmentation whose two labels share an exact centroid (one label painted as a concentric core inside another) produces no finding of any kind once item 129 makes the pipeline degrade instead of raise: Stage 3 is absent, `features.stage3_unavailable` records the cause, and every rule that reads `stage3` short-circuits on the missing block, so the verdict is whatever Stage 1/2 alone decides. `detect_overlaps` cannot see it either — a single-valued label map has no overlapping voxels by construction. So a genuinely collapsed/duplicated label set can pass. Item 129 deliberately adds no rule (its scope is degradation, not detection); the natural follow-up is a rule consuming `stage3_unavailable`, which would also give that key its `FEATURE_DOCS` catalogue entry *(item 129, 2026-08-31)* → roadmap.md Backlog — 'A collapsed or duplicated label set can pass silently once Stage 3 degrades on coincident centroids' (2026-09-01 triage)
- [x] gap — `src/segfacet/features/sagittal_projection.py` (item 021, `render_sagittal_projection`) is referenced by nothing outside its own module: not `pipeline.py`, not `feature_report.py`, not `cli.py`, not any rule, not `scripts/`. Confirmed 2026-08-31 by grep over `src/` and `scripts/` while surveying `SplineFit` consumers for item 130 — only `tests/test_021_sagittal_projection.py` calls it. This is the same dead-wiring shape Stage 26 D8 raised for `features/neighbourhood.py` (implemented in full, reachable from nothing, so absent from the generated catalogue and unable to influence any verdict), which item 110 resolved by wiring it in. `sagittal_projection` differs in that it renders a PNG rather than producing record leaves, so "wire it in" may mean a CLI flag rather than a pipeline call — but the choice between wiring it and retiring it has never been made, and no roadmap deliverable owns it *(item 130, 2026-08-31)* → roadmap.md Backlog — 'features/sagittal_projection.py (item 021) is reachable from nothing' (2026-09-01 triage)
- [x] gap — item 130's spec Validation section prescribes `segfacet run --seg <fixture> --out <dir> --no-reference` (no `--scan`) to confirm the CLI still produces a `stage3` block, but `cli.py`'s `_handle_run` now requires both `--scan` and `--seg` together — a bare `--seg` errors "provide --scan and --seg (single case) or --dataset-schema (batch mode)". This constraint post-dates the spec text (CLAUDE.md's own worked CLI example already shows `--scan` alongside `--seg`); the fix is trivial (pass `--scan` pointed at any valid NIfTI, even the seg fixture itself, since only the `stage3` block shape is under test) but any future item copying this Validation recipe verbatim will hit the same error *(item 130, 2026-08-31)* → docs/aide/items/130-one-closest-point-search-one-in-sample-fit.md Decisions & Trade-offs, dated correction 2026-09-01
- [x] defect — `synth/clean_gt.py::build_clean_spine` stacks ascending labels along **ascending** axis 2, so every synthetic fixture is anatomically inverted in S: `tests/corpus/fixtures/clean_control_seg.nii.gz` places `L1` at `S = 27 mm` and `L5` at `S = 187 mm` (measured 2026-08-31), where real anatomy has `L5` inferior to `L1`. Since `pipeline.extract_feature_record` orders centroids by ascending label — head-to-tail per item 093's convention — every in-repo driver advances *superiorly* while real VerSe input advances *caudally*. That single inversion is why item 131's traversal-direction defect (`tangent_angles_deg[]` reading ~5° in-repo and ~175° on real data) stayed invisible for nine items, and it silently flips the sign of any future feature measured against `+S`. Item 131 fixes the one array and cannot fix the corpus: correcting the stacking would move committed values across the whole suite plus both reference artifacts. Needs its own item, and Stage 20's specificity baseline should be pinned after it, not before *(item 131, 2026-08-31)* → roadmap.md Backlog — 'The synthetic fixture corpus is anatomically inverted along S' (2026-09-01 triage)
- [x] knowledge — item 131 spec AC16 says "the six `stage3.curvature.*` paths by name," but the committed catalogue (`docs/aide/feature_catalogue.generated.json`) carries eight `stage3.curvature.*` leaf paths (the four unsigned/signed angle arrays plus `total_curvature_deg`, `coronal_curvature_deg`, `sagittal_curvature_deg`, `curvature_plane`), measured 2026-08-31 against the pre-item tree. The test module pins the real set of eight rather than the spec's literal count of six, since the substantive AC16 property (no path added or removed) is unaffected by the miscount *(item 131, 2026-08-31)* → docs/aide/items/131-normalise-tangent-angles-for-traversal-direction.md Decisions & Trade-offs, dated correction 2026-09-01
- [x] knowledge — `roadmap.md`'s Stage 29 D8 and `queue/queue-018.md`'s item 132 both say the mode-4 monotonicity finding would reach `run_qc` "through the `sequence` rule". It does not and never could: `heuristics/sequence.py::SequenceRule` reads `relationships.out_of_order_labels` and never touches `monotonic_consistency`. The rule that consumes `stage3.monotonic_consistency.non_monotonic_pairs` is `heuristics/mislabel.py::MislabelRule`'s Detector B (`_detect_order_inconsistency`, `rule_id == "mislabel"`) — which is also what `tests/corpus/manifest.json` has always recorded as mode 4's `expected_rule_ids`. Verified 2026-08-31 by prototyping the fix and measuring `run_qc` on `mode4_relabel_swap`: one `mislabel` finding on labels {21, 22}, reason "Vertebra ordering inconsistent with label: ...". Item 132's spec records the correction; the queue and roadmap prose still name the wrong rule *(item 132, 2026-08-31)* → queue/queue-018.md item 132, dated correction 2026-09-01 — roadmap Stage 29 D8 names no rule, so the claim's roadmap half did not hold
- [x] knowledge — `feature_docs.STATUS_OVERRIDES`' maintainer note for `stage3.monotonic_consistency.is_monotonic` reads "Should be wired into the sequence rule directly; sequence-related checks should typically verify order along the spline parameter, not only label order," but the second half already holds and the first half names the wrong rule: `MislabelRule`'s Detector B has consumed `non_monotonic_pairs` since item 033, and after item 132 that path fires end-to-end through `run_qc`. The note is maintainer-signed text that no item may rewrite from inside (queue-018's scope fence), so it stands as written and will keep reading as an open action that is in fact closed. Needs a maintainer pass over the `monotonic_consistency` overrides, or the same treatment Stage 29 D11 gives the decision table's live counts: separate what is measured from what is signed *(item 132, 2026-08-31)* → roadmap.md Backlog — 'Maintainer pass over feature_docs.STATUS_OVERRIDES' monotonic_consistency notes' (2026-09-01 triage)
- [x] knowledge — VerSe19's committed masks are not stored RAS-resolving, and any measurement script that reads one with a bare `nibabel.load` instead of `segfacet.io.load_volume(path, integer_labels=True)` silently measures the wrong axes: `centroid_mm[2]` is then not the superior-inferior axis. Measured 2026-08-31 while specing item 132 on the first eight VerSe19 training subjects — through `load_volume` the ascending-label net S advance runs -26 to -364 mm (caudal-first, as real anatomy demands), through `nibabel.load` the same eight report -6.5 to +2.1 mm, and a traversal-order monotonicity check that trusted the raw axes called seven of the eight non-monotonic. The reorientation is item 094's contract and every in-pipeline consumer is downstream of it, so this bites only ad-hoc scripts and cohort sweeps — the class of code that never runs in CI. Worth a documented helper (or a line in `dataset-verse19.md`) so the next cohort measurement does not have to rediscover it *(item 132, 2026-08-31)* → docs/reference-build.md 'Staging the cohort for ingestion' — load_volume-not-nibabel.load note
- [x] gap — item 132's Authorised paths list six/seven test modules to reconcile for the mode-4 detection flip (test_125/039/129/098/123/040/057), but `tests/test_125_stage28_validation.py`'s own AC15 tests (`test_ac15_manifest_pipeline_detected_mode_count_is_six`, asserting `len(modes) == 6` and `modes == {1, 2, 3, 5, 6, 7}`) are not in that list and hardcode the pre-132 six-pipeline-mode count -- they will fail once mode 4 moves to `detection == "pipeline"`, unlike the two adjacent AC15 tests in the same module that compare dynamically against `test_040`/`test_057` and self-heal. A future item (or this one's builder, if the scope is widened) needs to flip the hardcoded `6`/`{1,2,3,5,6,7}` to `7`/`{1,2,3,4,5,6,7}` *(item 132, 2026-08-31)* → resolved within item 132 — tests/test_125_stage28_validation.py AC15 now asserts seven pipeline modes
- [x] framework — a second item (132, following 126) hit the same `aide scope` false positive: a spec that lists `docs/aide/progress.md` under "Asserts against" always FAILs scope, because the mandatory `aide progress set NNN in-progress` step every item runs commits a real change to that file, and the contradiction check in `cmd_scope`/`scope_findings` (`.aide/scripts/aide.py`) runs independent of the `_ALWAYS_AUTHORISED` always-list. Worth a framework fix: either exempt `_ALWAYS_AUTHORISED` paths from the contradiction check too, or have `aide-create-item` warn/refuse when a spec pins `progress.md` under Asserts against *(item 132, 2026-08-31)* → resolved upstream — engine 1.23.0 (aide-loop #90) warns at spec time when a spec pins an always-authorised path; installed here by PR #59, and aide check now names items 126/132
- [x] knowledge — TPTBox 0.7.6 corrects only its free-text `License:` metadata field (AGPL v3.0 -> `Apache License Version 2.0, January 2004`, upstream PR #119); it still ships `Classifier: License :: Other/Proprietary License`, unchanged from 0.7.5. Measured 2026-08-31 by reading both wheels' METADATA. Any licence scanner or SBOM tool that keys on trove classifiers rather than the `License:` field will still report FACET's core dependency as proprietary after item 133's bump, so the bump closes the AGPL claim but not the classifier one, and the residue is upstream's to fix *(item 133, 2026-08-31)* → docs/tptbox-install-numpy1.md header note on 0.7.6 licence metadata
- [x] knowledge — `pyproject.toml`'s own `[project.dependencies]` comment states that "exact reproducibility pins belong in the deployable container's lockfile/constraints (Stage 9), not in library metadata", yet `tptbox` is exact-pinned there (`==0.7.5`, now `==0.7.6`) — item 094 AC1's deliberate choice, enforced by `tests/test_094_tptbox_image_layer.py::test_ac1_tptbox_pin_is_exact_not_a_range`. The file has therefore contradicted itself since Stage 13, and every tptbox bump must edit two files plus five test literals instead of one lockfile line. Either the comment should record the tptbox exception and why, or the library bound should loosen to a range with the exact pin left to `constraints.txt` *(item 133, 2026-08-31)* → already recorded — pyproject.toml's dependency comment carries the tptbox exception (item 133, 2026-08-31)
- [x] framework — `aide check`'s `.gitattributes` eol-pin lint (`gitattributes_eol_pin_warnings` feeding `_byte_exact_reads`, `.aide/scripts/aide.py`) only resolves a committed path read directly inside an `==`/`!=` comparison, so a committed byte-reproducible text artifact that its consumers *parse* rather than byte-compare (JSON read through `json.loads`, a Markdown table parsed cell-by-cell) gets no warning whether or not it is pinned — the lint is silent in both directions, and a project can add an unpinned committed text artifact with a clean `aide check`. Found 2026-08-31 while specing item 134's new `docs/aide/golden_evidence.generated.json`, whose AC7 lint check is therefore honestly vacuous and whose pin has to be asserted by a project test instead. Worth either widening the lint to any committed text file a test resolves and reads, or documenting the limit beside the "precision over recall" docstring so a consumer does not read silence as coverage *(item 134, 2026-08-31)* → aide-loop#124
  - **2026-09-01** (pending handover) → aide-loop issue drafted at the 2026-09-01 feedback-loop triage, not yet filed — filing needs a human to confirm; the draft names engine 1.21.0 as the observed version
  - **2026-09-02** → answered upstream twice, both landed here with the 1.34.0 install. Engine 1.29.1 took this entry's *document the limit* option and explicitly declined to widen the lint; engine 1.29.4 then disproved that reasoning under review — immunity to the CRLF rewrite is a property of `read_text()`'s universal-newline translation, not of parsing — and took the *widen* option instead: **any** `read_bytes()` on a committed path is now reported, `read_text()` still only where its result is compared or hashed, with membership and ordering tests exempt. Measured upstream across four suites as adding zero warnings and removing none, which holds here: `aide check` reports no eol-pin warning at 1.37.0. Consequence for this repo: item 134's Assumptions block states the pre-1.29.4 mechanism ("fires only on a read sitting directly inside an `==`/`!=` comparison") and is now falsified as written, though its conclusion — the lint will not resolve `golden_evidence.generated.json`, so AC6 is the real enforcement — still holds. The assumption carries no `(engine X.Y.Z)` marker, so engine 1.36.0's stale-assumption check is silent on it.
- [x] knowledge — a "fails before the fix" replay that checks out a parent commit must run in a **separate clone with its own venv**, not in the working checkout: `pip install -e .` installs a meta-path finder that resolves `segfacet` ahead of `sys.path`, so a `PYTHONPATH`-pointed second tree is silently shadowed by the working checkout's `src/` and every pre-fix result is invalidated with no error at all. Using `git switch --detach` in the working checkout avoids the shadowing but detaches HEAD mid-item. Noted 2026-08-31 while specing item 135's per-defect verification, which reuses its fresh-clone rig for exactly this reason. Worth stating in `.aide/conventions.md` beside the stage-validation guidance so each stage-validation spec does not have to rediscover it *(item 135, 2026-08-31)* → CLAUDE.md Gotchas — 'A fails-before-the-fix replay needs a separate clone with its own venv'
- [x] defect — `tests/test_126_golden_retirement.py::test_ac17_no_live_reference_outside_allowlist[tests/corpus/golden]` fails in a clean fresh-clone full-suite run on this branch's own tip: `_AC17_ALLOWLISTED_FILES` names only `tests/test_116_ras_native_corpus.py` and `tests/test_126_golden_retirement.py`, but item 135's own `tests/test_135_stage29_validation.py` legitimately references the literal string `"tests/corpus/golden"` — both in prose (docstring, comments) and inside its AC2 assertion `"tests/corpus/golden" not in source`, which checks that *other* modules stay clean of the retired path. Measured 2026-08-31: reproduces identically in the working checkout and in a throwaway clone, so it is not an artefact of either tree. Fixing it means editing `test_126_golden_retirement.py` to add `test_135_stage29_validation.py` to the allowlist, which is outside item 135's authorised paths (`tests/test_126_golden_retirement.py` is listed read-only, "Read, never edited") — recorded here rather than fixed *(item 135, 2026-08-31)* → fixed in commit a496cae on aide/135-validate-stage-29-golden-retirement — tests/test_126_golden_retirement.py's _AC17_ALLOWLISTED_FILES gained this item's validation module, authorised by the spec's May-change list
- [x] defect — `tests/test_135_stage29_validation.py::test_adv_synthetic_deliverable_bullet_status_line_is_flagged`'s literal `offending_line = "✅ **D12** A hand-typed deliverable that was never through aide progress set."` is missing the leading `"- "` bullet marker that `_DELIVERABLE_BULLET_STATUS_RE` (`^-\s+[✅🚧📋⏸️❌⏳]\s*\*\*[A-Za-z]`) requires, so `_DELIVERABLE_BULLET_STATUS_RE.match(offending_line)` is `None` and the test fails unconditionally, independent of any `progress.md` content or environment. Measured 2026-08-31, reproduces identically in the working checkout and in a throwaway clone. Item 135 may not edit its own committed test module once written by the test-writer; the fix is prefixing the literal with `"- "` *(item 135, 2026-08-31)* → fixed in commit a496cae on aide/135-validate-stage-29-golden-retirement — tests/test_135_stage29_validation.py's offending_line literal in test_adv_synthetic_deliverable_bullet_status_line_is_flagged gained the leading '- ' the regex requires
