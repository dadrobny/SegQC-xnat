# constraints.txt — pinned dependency versions for the segfacet Docker image (item 066).
#
# Generated (not hand-written) by resolving the project's core runtime
# dependency graph in a clean environment:
#   python -m venv .venv-lock
#   .venv-lock/Scripts/pip install .      # (project-only install, no [dev]/[radiomics])
#   .venv-lock/Scripts/pip freeze
# ...then filtering the freeze output down to the seven declared core
# dependencies (numpy, scipy, scikit-image, nibabel, PyYAML, jsonschema,
# tptbox) and their transitive dependencies. Packages that are exclusively
# pulled in by the `dev` extra (pytest and its own transitives) are excluded,
# as is the optional `radiomics` extra (pyradiomics and its own transitives)
# — see AC5/AC7. matplotlib, previously dev-only, is now also a direct
# transitive of TPTBox (item 094) and so is included below. The one
# deliberate exception to the dev-extra rule is the CI test-runner pin at the
# end of this file; see the comment there.
#
# Consumed by the Dockerfile via: pip install -c constraints.txt .
#
# This file is an ordinary committed text artifact (not a byte-identity
# golden fixture); it should be regenerated whenever core dependency versions
# are deliberately bumped.

# --- core declared dependencies (pyproject.toml [project.dependencies]) ---
numpy==2.4.6
scipy==1.17.1
scikit-image==0.26.0
nibabel==5.4.2
PyYAML==6.0.3
jsonschema==4.26.0
tptbox==0.7.6

# --- transitive dependencies of the core stack ---
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.4.9
connected-components-3d==3.29.0
contourpy==1.3.3
cycler==0.12.1
dill==0.3.9
fastremap==1.20.0
fill-voids==2.1.2
fonttools==4.63.0
idna==3.18
ImageIO==2.37.4
importlib_resources==7.1.0
joblib==1.5.3
jsonschema-specifications==2025.9.1
kiwisolver==1.5.0
lazy-loader==0.5
matplotlib==3.11.1
narwhals==2.24.0
networkx==3.6.1
packaging==26.2
pillow==12.3.0
pynrrd==1.1.3
pyparsing==3.3.2
python-dateutil==2.9.0.post0
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
scikit-learn==1.9.0
simpleitk==2.5.5
six==1.17.0
threadpoolctl==3.6.0
tifffile==2026.3.3
tqdm==4.69.1
typing_extensions==4.16.0
urllib3==2.7.0

# --- CI test-runner pin (not part of the image's runtime graph) ---
# pytest-xdist is a `dev`-extra package and so is NOT installed by the
# Dockerfile's `pip install -c constraints.txt .` (a constraint pins a
# version, it never pulls a package in) — the image's runtime graph above is
# unchanged. It is pinned here because `.github/workflows/ci.yml`'s `test`
# job installs `-e .[dev] -c constraints.txt` and every `python -m pytest`
# step now runs `-n 4`: an unpinned runner would let a future xdist release
# change how the suite is scheduled without a commit here saying so.
pytest-xdist==3.8.0
