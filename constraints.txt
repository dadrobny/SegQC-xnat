# constraints.txt — pinned dependency versions for the segqc Docker image (item 066).
#
# Generated (not hand-written) by resolving the project's core runtime
# dependency graph in a clean environment:
#   python -m venv .venv-lock
#   .venv-lock/Scripts/pip install .      # (project-only install, no [dev]/[radiomics])
#   .venv-lock/Scripts/pip freeze
# ...then filtering the freeze output down to the six declared core
# dependencies (numpy, scipy, scikit-image, nibabel, PyYAML, jsonschema) and
# their transitive dependencies. Packages that are exclusively pulled in by
# the `dev` extra (pytest, matplotlib, and their transitives) are excluded, as
# is the optional `radiomics` extra (pyradiomics/SimpleITK) — see AC5/AC7.
#
# Consumed by the Dockerfile via: pip install -c constraints.txt .
#
# This file is an ordinary committed text artifact (not a byte-identity
# golden fixture); it should be regenerated whenever core dependency versions
# are deliberately bumped.

# --- core declared dependencies (pyproject.toml [project.dependencies]) ---
numpy==2.0.2
scipy==1.13.1
scikit-image==0.24.0
nibabel==5.3.3
PyYAML==6.0.3
jsonschema==4.25.1

# --- transitive dependencies of the core stack ---
attrs==26.1.0
imageio==2.37.2
importlib_resources==6.5.2
jsonschema-specifications==2025.9.1
lazy-loader==0.5
networkx==3.2.1
packaging==26.2
pillow==11.3.0
referencing==0.36.2
rpds-py==0.27.1
tifffile==2024.8.30
zipp==3.23.1
